# .github/dependabot.yml
version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    target-branch: "test"
    # Grouped into one PR. Without this these two repos opened a separate PR per
    # dependency - visible all over their merge history (#18).
    groups:
      pip-deps:
        patterns:
          - "*"
    ignore:
      # Pinned in requirements_llm.txt for tested compatibility with the
      # vLLM/transformers guided-decoding stack — bump these by hand after
      # verifying against the model registry, not via automated PRs.
      - dependency-name: "torch"
      - dependency-name: "vllm"
      - dependency-name: "bitsandbytes"
      - dependency-name: "transformers"
        versions: [">=5.0.0"]

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    target-branch: "test"
    # Grouped into one PR. Without this these two repos opened a separate PR per
    # dependency - visible all over their merge history (#18).
    groups:
      actions:
        patterns:
          - "*"
