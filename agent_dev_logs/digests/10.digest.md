# 📓 Digest — [\#10](https://github.com/ufal/atrium-llm-enrich/issues/10) `open`: PDF and DOCX inputs handling for DU

> _Condensed from [`../issues/2026-07-17.10.issue.open.md`](../issues/2026-07-17.10.issue.open.md)
> (export regenerated 2026-07-22, incl. both 2026-07-21 comments; the 2026-09-24 export on `test` `c1ad762` adds
> stranak's 2026-09-23 question) and the repo's `test` branch (refreshed 2026-09-24 against `951db5e`, with a measured
> comparison of the three routes stranak named).
> Full survey + routing + implementation record: [`../plans/10.plan.md`](../plans/10.plan.md)._
> _Companions: format-decision issue [#11](https://github.com/ufal/atrium-llm-enrich/issues/11)
> ([`11.digest.md`](11.digest.md)) and the hub DU **evaluation** track
> [ufal/atrium-project#22](https://github.com/ufal/atrium-project/issues/22)._

**Labels**: `development`, `integration` · **Type**: Task · **Milestone**: Q4 [WP3: Conclusion of Semantic Harmonization]
**Status (2026-09-25)**: ✅ **Next steps 1 and 2 of the 09-24 answer done in the working tree** (plan §11, with #18 §0e; files in chat, not pushed): every G1–G8 gap closed on the JSON route, the clients' auto-convert switched to it, the legacy converters deprecated (not deleted, by decision); an opt-in Docling engine for complex layouts. Step 3 (delete ~800 lines) is a later round.

_Status before:_ 🛠️ **Implemented on `test`.** First pass
([`456ebb2`](https://github.com/ufal/atrium-llm-enrich/commit/456ebb2) "add pdf/docx to md scripts"):
a **visually-rich Markdown converter for DOCX + digital-born PDF** per the issue's 2026-07-21
comments. Second pass (2026-07-22, plan §7): **unified the annotated-MD schema over TEITOK/ALTO**
(`xml_to_md.py --format layout`), added an **opt-in Tesseract OCR path** for scanned/curve pages,
**page-level citations** (schema `page` field), and **pipeline auto-convert** of `.pdf`/`.docx`. The
2026-07-19 research pass (survey + routing) stands. OCR *tool selection* (PERO vs Tesseract) stays
hub-#22-benchmark-gated. flexiconv is no longer licence-*unknown*: its `pyproject.toml` at the pinned
`v0.3.10` declares **GPL-3.0-or-later** (checked 2026-09-23; still no LICENSE file), so enabling it is a
project-policy decision, not an open question about the upstream. Since `f62921c` (on `test`, 2026-09-23) it is
recorded as a conditional `para_config.txt` component, and the nlp-enrich reader/adapter that make its output usable
are re-vendored (below). Round 4 (2026-09-24) found that a `.teitok.xml` given as line-level input enriched
**zero lines**, and fixed it the same day (on `test` since `951db5e`). **Route to Markdown decided (2026-09-24):**
stranak's question (2026-09-23) was answered from a measured comparison — c) our converters, narrowed to the JSON route
of #18; b) flexiconv for the TEITOK plane; a) markitdown not adopted (reply text in plan §9,
posted on the issue 2026-09-24 11:20 ([5813071038](https://github.com/ufal/atrium-llm-enrich/issues/10#issuecomment-5813071038))). The measurement also found that the JSON route is **not at parity** with the older direct-to-Markdown
converters the clients still use (gaps G1–G8 below); closing that is the remaining #10 work.

## 🎯 Goal
Turn input **PDFs (curve-only / scanned / digital-born)** and **DOCX** into the repo's LLM-friendly
targets — since the 2026-07-21 comments, specifically into **visually-rich Markdown**: page borders
and layout cues recorded as HTML comments inside page-sectioned `.md`, so the LLM keeps layout
signal a plain-text dump would lose.

## 🧵 Thread trail
- **2026-07-17, K4TEL** — opened: 3 PDF classes (curves / scans / digital-born), DOCX as an
  alternative input, DOCX/HTML as possible intermediates.
- **2026-07-19 (repo)** — research pass: tool survey, routing, smoke tests (plan §1–§5).
- **2026-07-21, K4TEL ×2** — steer to implementation: use PDF-to-MD / DOCX-to-MD tools, record
  **page borders + as many visual layout cues as possible as comments in the MD**; posted the
  **exhaustive cue taxonomy** with exact encodings (`<!-- PAGE_BREAK -->`, `<!-- BBOX -->`,
  `<!-- FONT -->`, `<!-- HEADER_START -->`, `~~strike~~`, footnotes, GFM tables, `<!-- WATERMARK -->`).
- **2026-07-22 (repo)** — converter landed (`456ebb2`); DEVLOG rebuilt + issue exports refreshed
  (`9003639`).
- **2026-07-23, K4TEL** — status: converter, unified schema, opt-in OCR, auto-convert on `test`.
- **2026-07-25, K4TEL** — the three targets: Markdown with cues as the LLM's input, the #13 JSON as the
  document store, TEITOK (flexiconv, then nlp-enrich) for the visual + NLP layer.
- **2026-09-23, K4TEL** — status: flexiconv's licence recorded, its output readable here (`f62921c`).
- **2026-09-23, stranak** — asks which route to Markdown: markitdown, flexiconv, or the custom converters.
- **2026-09-24 (repo)** — round-4 fixes on `test` (`951db5e`); the three routes plus our two paths and alto-postprocess #31's
  reader measured on 9 inputs (markitdown 0.1.8, flexiconv 0.3.10 = `main`); direction decided, reply drafted (plan §9–§10).
- **2026-09-24 11:20, K4TEL** — the answer is posted (5813071038): c) kept narrow, b) for the TEITOK side, not a);
  next: close the ⚠ gaps, switch the auto-convert to the JSON path, delete the legacy converters. The gap register
  and work plan are in `08dff48` (plan §9–§10).

## 🛠️ What landed (`456ebb2`)
- **`api_util/layout_md.py`** — dependency-free **cue vocabulary**: pure emitters returning the
  taxonomy's exact strings + a `CUE_SCHEMA` catalogue ("the list of all possible visual layout
  pieces", single-sourced and testable).
- **`api_util/docx_to_md.py`** (python-docx, MIT) — styles→headings, emphasis + explicit FONT/STYLE
  cues, alignment, tables, headers/footers + margins, page breaks → `## Page N`, images, footnotes.
- **`api_util/pdf_to_md.py`** (pdfplumber, MIT) — per-page `DOC_META`, block `BBOX`+`FONT` (blocks
  split on gaps *and* font-size change), tables, **decode-sanity check** → text-less/garbled pages
  emit `NEEDS_OCR` instead of fabricated text.
- **`api_util/doc_to_visual_md.py`** — dispatcher + CLI: pre-convert, land the `.md` in `INPUT_DIR`,
  run the pipeline unchanged (HTML-comment cues pass through as inert text).
- Optional `requirements_docmd.txt`; 4 hermetic test modules.

## 🛠️ What landed (plan §7, 2026-07-22 — second pass)
- **Unified schema:** `xml_to_md.py --format layout` renders TEITOK/ALTO to the *same* annotated MD
  (DOC_META/BBOX/PAGE_BREAK/figure cues) from their coordinates — annotated MD is now the one diet
  across PDF, DOCX, and TEITOK/ALTO. Default `markdown`/`text` output byte-identical.
- **OCR (opt-in):** `pdf_to_md.py --ocr` renders text-less pages (pypdfium2) and transcribes them
  with Tesseract `ces` (`pytesseract`), tagged `<!-- OCR: engine=tesseract, lang=ces -->`; graceful
  fallback to `NEEDS_OCR` when the binary is absent. Default behaviour unchanged.
- **Citations:** the document schema returns an optional `page`; `run_document_level` surfaces it →
  `[Source: <doc_id>, Page N]` (issue #11 §3.3).
- **Auto-convert:** `llm_client_shared.prepare_document_input()` converts `.pdf`/`.docx` to a cached
  `.md`, wired into the openrouter/ollama clients (both gain `--ocr`). Suite **268 passed, 3 skipped**.

## 🧭 Research findings that still steer the rest (plan §1–§5)
- **Three PDF classes → two paths** via a cheap font+char+image census; **curves and scans are the
  same OCR path** (`curves.pdf` extracted 0 chars; Tesseract `ces` recovered the text).
- **4th class "extracts but garbled"** (no `/ToUnicode` → `sondě→sondI`) — now guarded by the
  converter's decode-sanity check; the valid-but-wrong-diacritics case needs a dictionary/LM
  hit-rate and rides with the OCR follow-up.
- **Recommended tools (permissive-first):** pdfplumber (landed) / PERO-OCR + Tesseract `ces` (OCR
  path, pending) / pandoc·mammoth (DOCX alternatives; python-docx landed) / Docling·MinerU (complex
  layouts — evaluate in #22). License traps: Marker (GPL + model revenue cap), PyMuPDF (AGPL),
  MinerU (custom), **flexiconv (GPL-3.0-or-later, declared in `pyproject.toml` only — policy decision)**.

## ⏳ Open / next
- ✅ **Route to Markdown — decided 2026-09-24** (answer to stranak; reply text in plan §9, posted on the issue 2026-09-24 11:20 ([5813071038](https://github.com/ufal/atrium-llm-enrich/issues/10#issuecomment-5813071038))):
  **c) narrowed**: one route, PDF/DOCX → `atrium_document` JSON (#18) → Markdown rendered by our cue layer (`layout_md.py`,
  `xml_to_md.rows_to_layout_markdown`, `json_to_md.py`, ~1,100 lines shared with the TEITOK path) over permissive readers
  (pdfplumber, python-docx); the legacy `pdf_to_md`/`docx_to_md` (~800 lines) retire. **b) flexiconv** for the TEITOK/NLP plane
  and the long tail of formats, as a GPL CLI step; its TEITOK reaches the LLM through `xml_to_md --format layout`. **a) markitdown**
  not adopted: no page markers, so no page citations (#11).
- 📏 **Measured 2026-09-24** (full matrix, setup and gap register in plan §9):
  - **markitdown 0.1.8** (MIT): DOCX good (mammoth; reads the Override-less `CTX000000010.docx`, keeps tracked insertions); PDF has no
    page markers, bbox or fonts, drops the image-only page silently, passes mojibake through, turns two-column prose into sparse tables.
    OCR only via Azure Document Intelligence.
  - **flexiconv 0.3.10** (GPL-3.0-or-later, `main` = `v0.3.10`): **writes no Markdown** (outputs teitok, tei, html, txt, docx, hocr,
    xpdf, rtf, conllu, srt, doreco, exb, raw; `io/md.py` only reads); no bbox with the default `pdf=smart`; no OCR; shares the
    python-docx gaps (G4, G5).
  - **Our JSON route** is ahead on broken text layers (`garbled.pdf` → `NEEDS_OCR`, which the legacy route misses) and behind on the
    rest — the gap register below.
  - **alto-postprocess #31** (`text_formats.py`) is a third in-house reader: it gets G1, G3 page breaks, G4, G5 and two-column order
    right, has no geometry and misses CP1250 mojibake.
- ⏳ **JSON-route gap register** (at `951db5e`; fixes in plan §10 Phase 1):
  - **G1** image-only page scored Clear/1.0, no `needs_ocr`, absent from the Markdown (`digital_to_json.py:377-403`).
  - **G2** glued words and merged columns — chars joined with `""` per `round(top)` row (`:497-505`).
  - **G3** DOCX `heading_level` / `tables[]` stored but not rendered (#18 PR 5.5); explicit DOCX page breaks not captured.
  - **G4** python-docx rejects a package without the `word/document.xml` Override (`CTX000000010.docx`).
  - **G5** tracked insertions (`<w:ins>`) dropped by `Paragraph.text`.
  - **G6** on a condemned page, single-misread lines still render (`GARBAGE_MIN_HITS = 2`, `:161`).
  - **G7** every PDF stamped `digital-born-pdf`, including OCR-layer scans (alto-pp #31 detects these) — code reading.
  - **G8** two entry points, two outputs: clients' `prepare_document_input()` (`llm_client_shared.py:839`) → legacy route; hub e2e →
    JSON route; `_visual_md_cache` keyed on mtime only.
- ⏳ **Phase 2 — one entry point** (#18 PR 7): auto-convert through the JSON route, `docx_to_md.py` + `digital_born/` deleted,
  cache stamped with the converter version; `--ocr` stays on `pdf_to_md` until the OCR hand-off exists.
- ⏳ **OCR hand-off and engine** — `needs_ocr` pages go to the OCR originator (alto-postprocess, per `ORIGIN_ORIGINATORS`); engine by the
  hub #22 bake-off with Tesseract `ces` (live, opt-in), PERO-OCR, #24 olmOCR, #25 archivatorium. `pdf_to_md.py` retires then.
- ⏳ **Shared text-layer classifier** with alto-postprocess #31 (covers G7), shipped like `atrium_document.py` through the hub template +
  para-drift (plan §10 Phase 4).
- ⏳ **Complex layouts** — two-column `sample.pdf` defeats all routes; Docling (MIT) / MinerU bake-off on the #22 harness, adopted
  only as an adapter into the JSON IR.
- **flexiconv** — GPL-3.0-or-later; services stay GPL-free (atrium-nlp-enrich #38, 2026-09-24); CLI production-run policy still the
  team's. `para_config.txt` row now says llm-enrich only vendors the adapter/reader and logs nothing (`951db5e`). G4/G5 reproducers can
  go upstream.
- ✅ (`f62921c`, `951db5e`) **flexiconv output is readable and enrichable** — re-vendored `teitok_read.py` / `flexiconv_convert.py`
  (pinned by `tests/test_vendored_teitok_parity.py`); a TEITOK row without a quality score is "unknown", not Trash; GPU path passes it.
- ✅ **README section** — "Visually-Rich Document Input"; to be updated with Phase 2.
- **Closure of #10:** reply posted + Phases 1–2 on `test`; the rest is tracked in #18, hub #22 / #24 / #25, #11 and the new cross-repo issue.

---
_Research pass 2026-07-19 · implementation landed 2026-07-22 (`456ebb2`) · digest refreshed
2026-07-22 against `test` HEAD `fdf3d74`; TEITOK/flexiconv parts refreshed 2026-09-24 against `test` HEAD `122915c`,
status of the round-4 fixes and stranak's question added the same day (`test` at `c1ad762`, dev-log commits only);
route decision, measurements and gap register added 2026-09-24 against `test` `951db5e`.
Companion: [`../plans/10.plan.md`](../plans/10.plan.md)._
