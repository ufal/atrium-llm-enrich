# 📓 Digest — [#18](https://github.com/ufal/atrium-llm-enrich/issues/18) `open`: Build explicit PDF/DOCX-to-JSON converter for digital-born documents

> _Condensed from [`../issues/2026-08-02.18.issue.open.md`](../issues/2026-08-02.18.issue.open.md)
> (export 2026-08-02) and the architectural strategy session, refreshed 2026-08-03 (second
> pass) against the actual state of the `test` branch and the hub._
> _Strategy + execution verdicts: [`../plans/18.plan.md`](../plans/18.plan.md)._
> _Companions: PDF/DOCX input handling [#10](https://github.com/ufal/atrium-llm-enrich/issues/10)
> and intermediate data format decisions [#13](https://github.com/ufal/atrium-llm-enrich/issues/13)._

**Labels**: `enhancement`, `development`, `integration`
**Status**: 🔓 **Architecture defined, PR 0 done, §1a resolved — PR 2/3 unblocked behind PR 0.5 and PR 1.** `atrium_document` JSON remains the first-class canonical IR; Markdown with HTML comments (Issue #10) is deprecated as an intermediate format. Since the first 2026-08-03 pass: the block-ownership question is **decided** (`BLOCK_OWNERS` gains tuple values; `source.origin` selects the originator per document), and implementing it surfaced three further defects — a hub/vendored **drift**, a **silent field-drop** that made the `group_id` landing lossy, and `group_id` being **inert** because no consumer reads it.

## 🎯 Goal
* Build an explicit converter (`api_util/digital_to_json.py`) that ingests digital-born `.pdf` and `.docx` files, extracts their structural and spatial layout formatting, and outputs them directly into the strict `atrium_document.schema.json` format for seamless LLM workflow integration.

## 🧵 Thread trail
* **2026-08-02** — Issue opened to bridge the visual layout cue taxonomy developed in Issue #10 with the new ecosystem-wide JSON standard.
* **2026-08-02** — Architectural review concluded that rewriting the old PDF/DOCX→Markdown scripts is a trap. The new flow bypasses Markdown entirely: `PDF/DOCX → Common Internal Python Model → canonical atrium_document.schema.json`.
* **2026-08-02** — Fidelity rules established: PDF provides the strongest spatial truth (bounding boxes, coordinates), DOCX the structural truth (paragraphs, runs, headings). DOCX adapters must not fabricate spatial bounding boxes.
* **2026-08-02** — Tool stack: Docling as the primary structural foundation, a PDF geometry library for coordinates, python-docx/docx2python as DOCX fallback. Routing detects digital-born documents before anything reaches OCR.
* **2026-08-03** — Repo check: Phase 0 prototyping already existed under `digital_born/` and had been exercised against `sample.pdf` — `sample.pdf_docling_ir.json` (197 KB) is real Docling output, not a stub. None of it was in the DEVLOG.
* **2026-08-03** — Same check: the legacy `api_util/*_to_md.py` converters had been relocated into `digital_born/`, breaking three test modules at collection; `requirements_docmd.txt` still described the old stack.
* **2026-08-03** — Schema reality check: no paragraph/text-block grouping node existed, and "block" was already the established term for the schema's top-level field-owned sections — so the plan's "Text block → `block`" row both collided with existing terminology and named nothing real.
* **2026-08-03** — Regression fixed; `lines[].group_id` landed; a second question surfaced (block ownership).
* **2026-08-03 (second pass)** — **§1a resolved**, and three defects found while implementing it: §0b drift, §1b silent field-drop, §1c inert `group_id`. Details below. The lesson worth carrying: a "✅ landed" claim in this digest needed auditing against the repo rather than trusting — the change recorded as landed had in fact landed in one of two byte-identity-enforced copies.

## 🧭 The answer in one picture
* **Separate extraction, unified serialization.** A `PDF Adapter` (Docling + geometry) and a `DOCX Adapter` (Docling + OOXML fallback) → `Normalized DigitalDocument` → `AtriumDocument Serializer` → `Schema Validation` → `LLM Enrichment`.
* **One originator role per block, chosen per document.** `pages`/`content`/`lines`/`tables` describe a document's *positional plane*, and there are two ways to acquire one — OCR/ALTO or direct digital extraction. They are mutually exclusive per document, so `BLOCK_OWNERS` lists both candidates and `source.origin` decides.
* **Validation is a gatekeeper, but not a sufficient one.** No `doc.json` is emitted if `jsonschema.validate()` fails — *and* a round-trip assertion runs first, because the schema requires only `page`+`line` on a `lines[]` row and therefore cannot catch a field-ownership drop.
* **Semantic vocabulary over raw HTML comments.** Legacy Issue #10 cues map to JSON nodes rather than being copied over. Coming back the other way, a `group_id` boundary renders as a **blank line** — Markdown's native paragraph primitive — so no new cue was needed at all.

## ✅ §1a resolved — how, and why not the two obvious ways

`BLOCK_OWNERS` was conflating *who is authoritative for a block's semantics* with *who happens to originate it first*. For the positional plane those differ, and the second question already had an answer in the record: **`source.origin`**, already first-writer-wins in `set_source()`, already carrying `digital-born-pdf` / `docx` / `ocr:…` / `ABBYY-ALTO` as examples.

So `BLOCK_OWNERS` values may now be a tuple, `ORIGIN_ORIGINATORS` maps an origin prefix to its authorized originator, and `_assert_origin_consistent()` enforces it on **both** `set_block()` and `merge_block()` — the latter mattering because `merge_block()` never called `_assert_owner()`, so `pages` and `lines` were never ownership-checked at all. The check is self-guarding, so it is a no-op for every pre-#18 caller.

* ❌ **No-op alto passthrough** — would stamp `assembled.blocks[…].program = "alto-postprocess"` and add a `provenance.contributors[]` entry for a run that did nothing, in a record built for FAIR catalogue export. Rule 4 exists for attribution granularity; this falsifies it. It also puts the alto container back on the digital-born critical path.
* ❌ **A second name with no further guard** — `content` and `tables` aren't field-split, so either originator could `set_block()` over the other's. Mutual exclusivity would stay an unwritten assumption.
* ✅ **Originator role + `source.origin`** — and *not* the `assembled.blocks` stamp, because rule 4 makes that stamp name the **most recent** writer, so nlp-enrich merging into `lines[]` erases the originator signal.

**No `SCHEMA_VERSION` bump.** Versioning rule 2 ("changing block ownership" → major) is amended to say why: **widening write authorisation is additive; re-attributing an already-written block would not be.** No field renamed or removed, every record still valid, every tool keeps every capability.

**`llm-enrich-digital` → `digital-convert`.** These identities are roles, not repos, and they land permanently in `provenance.contributors[].program` on export.

## 🚨 Three defects found while implementing the decision

### §0b — the `group_id` change landed in the hub copy only
`para-drift.reusable.yml` `diff -u`s `atrium_document.py` **and** `atrium_document.schema.json` against every tool repo. Both pairs currently differ: `group_id`, `tables`, `forms` and the digital field grant are in `docs/templates/shared/` and absent from the llm-enrich root copies. Either red now or red the instant `v1` moves — and per #13's TODO list **no repo runs pytest in CI**, so para-drift is the only gate that would catch it. Separately, `tables` and `forms` were added to the hub schema and module with no entry in the plan, this digest, or `docs/document_schema.md`, whose ownership table still lists five tools and neither block.

### §1b — as first landed, §1 silently dropped all text and bbox
`BLOCK_FIELD_OWNERS["lines"]["llm-enrich-digital"] = ["group_id"]` meant `merge_block()` filtered `text` and `bbox` out **with no warning** (`_complain()` only fires when the grant is `None`), and the result still **passed** `jsonschema.validate()`, because `lines[]` requires only `page`+`line`. It surfaced three stages later as `json_to_md`'s `ValueError("nothing to render")`. Also: `lines[].bbox` had been granted to `nlp-enrich` and not to any originator, so the PDF adapter — whose whole point is exact coordinates — could not write the field that carries them. Both grants corrected; `bbox` is now deliberately granted to two writers who can never meet on one document, enforced rather than assumed.

### §1c — `group_id` was inert
`json_to_md._rows_from_lines` builds `{page_num, line_num, text}` + optional `bbox` and drops everything else, so the paragraph fidelity §1 bought never reached the model. Fixed by carrying `group_id` through and rendering a boundary as a blank line. `tables` rendering (grid shape joined back to line text via `group_id`, through `layout_md`'s existing `md_table`) is the remainder of PR 5.5 — deliberately not stubbed, since a half-rendered table is worse than the grouped lines the model already gets.

## 🛠️ Landed
* **Architectural contract** — ✅ the four-layer strategy (Extraction → Normalization → Serialization → Validation) is locked in.
* **Tool stack (decision)** — ✅ Docling as the primary structural abstraction. The PDF **geometry** library is now an open licence decision, not a settled choice (see below).
* **Phase 0 prototyping** — ✅ probes exist and have been exercised against `sample.pdf`/`sample.docx`; deps declared in `requirements_digital.txt`.
* **Regression fix** — ✅ broken `api_util` imports restored; full suite green (304 passed, 6 skipped, 0 failed) — *locally; CI runs no pytest*.
* **`lines[].group_id` schema extension** — ⚠️ landed in the **hub canonical only**. Five vendored copies and a `v1` move pending (§0b). This entry previously read "✅ landed in `atrium_document.schema.json` + `atrium_document.py`", which was true of one of the two enforced copies.
* **§1a block ownership** — ✅ decided, with the rejected alternatives recorded so it isn't relitigated.
* **Golden fixtures** — ✅ `tests/fixtures/digital/` generated by `make_fixtures.py`, sha256-pinned, `--verify` mode. `minimal.pdf` 1039 B / `garbled.pdf` 722 B / `minimal.docx` 2366 B, all validated (`qpdf --check` clean, exact bboxes via pdfplumber, structure via python-docx).

## ⏳ Open / next
* **PR 0.5 — re-vendor + docs** 🆕 blocks everything else. Re-vendor both canonical files into all five tool repos, update `docs/document_schema.md` (Contract paragraph, ownership table, `tables`/`forms`, versioning-rule amendment, changelog), move `v1`. `scripts/revendor_shared.sh --check` first.
* **PR 1 — contract.** `BLOCK_OWNERS` tuples, `ORIGIN_ORIGINATORS`, `_assert_origin_consistent()`, corrected `digital-convert` grants, the `llm-enrich-digital` rename, `tests/test_document_originators.py`.
* **Font/style target field** — the mapping table's font row still has nowhere to go. Recommend mapping only *semantic* style (bold/italic/heading) and dropping typeface, since `categ` already carries heading-ness and style is what a downstream reader can act on. **Decide in PR 1** — leaving it open into PR 3 is how §1 became ambiguous the first time.
* **PDF geometry licence decision** 🔴 sharper than "flag for review": rule 5 merges component licences as a **most-restrictive union** into `provenance.license`, so declaring PyMuPDF (AGPL-3.0) sets `provenance.license = AGPL-3.0` for **every digital-born document**, and that is what goes to catalogue export. Docling already owns structural grouping, so PyMuPDF's unique contribution is only span geometry — and pdfplumber (MIT, already vendored) returns char-level geometry to three decimals plus font, size and matrix, with block gaps detectable at 2.0 pt vs 34.0 pt. Run the A/B on `sample.pdf` (protocol in `digital_born/README.md`) before PR 3.
* **`merge_block()` dropped-field warning** — the ecosystem fix for §1b's class of defect, highest leverage of anything here, but **not free**: at least one existing call site passes fields it doesn't own for readability, so it needs a call-site cleanup pass. Its own PR, not bundled with #18.
* **Probe rename** — `digital_born/test_*.py` → `probe_*.py`. They are pytest-collectable, import optional deps not in the base install, and are not tests.
* **Phase 2–4 development** — internal representation, PDF/DOCX adapters, serializer. Unblocked once PR 0.5 + PR 1 land.
* **PR 5.5 — consumer** 🆕 `group_id` then `tables` in `json_to_md`/`xml_to_md`. Without it PR 1's fidelity gain never reaches the model.
* **Phase 5–7 validation** — golden tests over the new fixtures, then pipeline compatibility against `atrium-llm-enrich`. Note #13 TODO 1: no repo runs pytest in CI, so "CI-tested" is aspirational until that changes.

---
_Issue opened 2026-08-02 · architecture 2026-08-02 · repo audit + regression fix 2026-08-03 ·
§1a resolved and three follow-on defects found 2026-08-03 (second pass). Companion:
[`../plans/18.plan.md`](../plans/18.plan.md)._
