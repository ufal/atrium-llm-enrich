# 📓 Digest — [\#11](https://github.com/ufal/atrium-llm-enrich/issues/11) `open`: Decide on inputs of LLMs based on benchmarks existing for DU

> _Condensed from [`../issues/2026-07-17.11.issue.open.md`](../issues/2026-07-17.11.issue.open.md)
> (export 2026-07-22) and the two **Gemini 3.1 Pro Deep-Research reports** attached to the issue
> (`Roadmap-LLM-Document-Retrieval.md`, `research-issue-11.md`). The earlier report's full text
> lived verbatim in this file until 2026-07-22 — recover it from this file's git history
> (`59f0ddb`/`f88b216`) or the issue attachments. Strategy + format verdicts:
> [`../plans/11.plan.md`](../plans/11.plan.md)._
> _Companions: input-ingestion [#10](https://github.com/ufal/atrium-llm-enrich/issues/10)
> ([`10.digest.md`](10.digest.md), implementation landed) and the hub DU benchmark
> [ufal/atrium-project#22](https://github.com/ufal/atrium-project/issues/22)._

**Labels**: `question`, `development`, `integration` · **Milestone**: Q4 [WP3: Conclusion of Semantic Harmonization]
**Status**: 🧭 **Decision drafted, mechanism partly built** — **annotated (visually-rich) Markdown**
as the single LLM input format, PAGE/ALTO/TEITOK as the spatial source of truth, HTML/DOCX/PDF as
inputs/intermediates only. The converter now emits that annotated MD for **PDF, DOCX *and*
TEITOK/ALTO** (`xml_to_md.py --format layout`), and the doc-level schema carries a `page` field for
citations (issue #10 §7, 2026-07-22). Pending empirical checks: **Docling/Marker PoC** on the
hardest PDFs and the **#22 format bake-off**.

## 🔄 Refresh 2026-09-24 (thread since 07-21, and what the repo did)
- **2026-07-23, K4TEL** — status comment (5054893293): decision drafted (annotated Markdown as the diet, HTML an
  intermediate, IDML skipped, PAGE/ALTO/TEITOK the spatial truth, TXT the fallback), mechanism partly built; next the
  Docling/Marker PoC and the #22 bake-off. The status below already said this.
- **2026-08-02, K4TEL** — **validation task** (5157069225): the three formats are designated (JSON for metadata and
  search, Markdown for layout-light ingestion, TEITOK for exact boxes), so stress-test `json-to-md` and `teitok-to-md`
  on fully enriched documents: compile stress fixtures (JSON and TEITOK with NER, overlapping cues, edge-case
  characters), run the converters, assert that cues are kept; acceptance: no unhandled exceptions, and "no entities,
  keywords or spatial comments lost". The same TODO closes hub #24's 2026-08-01 three-format decision.
- **State of that task (2026-09-24):** both converters exist and are tested case by case, not with a stress suite.
  `api_util/json_to_md.py` (from #18; 20 tests: page sections, bbox cues, group boundaries, non-numeric labels,
  `canvas.unit`, `needs_ocr` reasons, fallback to `content.text`) and `api_util/xml_to_md.py` (25 tests, rebuilt on
  TEITOK format 2's page model in round 4: page labels, a sentence over a page break, print-space boxes, flexiconv
  output without `<s>`, the legacy `</n>` close). The real writer sample `CTX000000002` (entities, a `<pb/>` inside a
  sentence) is a pinned fixture. **The fidelity criterion needs re-scoping:** by design `json_to_md` never reads the
  `enrichment` block (the model would read its own earlier answer) and neither renderer writes `<name>` entities or
  keywords into the Markdown, so "no entities or keywords lost" cannot hold for the Markdown view; it holds for the
  JSON record, where they live. What a stress suite should assert is that text, pages, line order and every spatial
  cue survive.
- **Since then in the repo:** #18's JSON route became the intermediate (Markdown is a view rendered from the JSON or
  from TEITOK); #10's route decision (answered 2026-09-24): our cue layer writes the Markdown, markitdown is not
  adopted (no page markers, so no page citations), flexiconv serves the TEITOK side. The Docling/Marker PoC and the #22
  bake-off have not run.

## 🎯 Goal
From the issue's format × detail matrix (PDF · InDesign XML · HTML+CSS · DOCX · PAGE/ALTO XML · MD ·
TXT), decide the format(s) that feed the LLMs — grounded in what existing DU benchmarks actually
use — toward a FAIR standard collection.

## 🧵 Thread trail
- **2026-07-17, K4TEL** — opened with the format matrix + two leads:
  [`atrium-page-classification`](https://github.com/ufal/atrium-page-classification) for per-page
  routing, and [Grobid](https://grobid.org/).
- **2026-07-20, K4TEL** — "Why Markdown wins": HTML = token bloat, TXT = structure loss, MD = the
  sweet spot; Docling/Marker for complex layouts (OTSL tables ≈80 % fewer tokens than HTML; LaTeX
  for math); **critical: inject `<!-- Page N -->` markers** for page-level citations; PoC
  recommendation. Attached the full DR report (`Roadmap-LLM-Document-Retrieval.md`).
- **2026-07-21, K4TEL** — DR summary (`research-issue-11.md`): MD as lingua franca · spatial
  provenance via embedded markers · Docling/Marker over legacy parsers (Grobid solid but older) ·
  FAIR alignment via hierarchical metadata "roadmaps" (`SKILL.md`/`INDEX.md`) navigated by an agent
  instead of chunk-RAG. **Conclusion: prioritise annotated Markdown with embedded page markers.**
- **2026-07-21, K4TEL** — cross-posted the visual-layout-cues idea → became issue #10's
  implementation (landed 2026-07-22, `456ebb2`).

## 🧭 The answer in one picture
- **Annotated MD is the diet.** Vanilla MD's gaps vs PAGE/ALTO (page borders, positions, fonts) are
  restored by HTML-comment cues at near-zero token cost — exactly what #10's
  `api_util/doc_to_visual_md.py` + `layout_md.py` now emit (`PAGE_BREAK`, `BBOX`, `FONT`, `STYLE`,
  headers/footers, GFM tables), a superset of the DR's bare `<!-- Page N -->` marker.
- **Benchmarks corroborate the two-track design** (plan §2): end-to-end parsing is scored **in MD**
  (OmniDocBench); recognition ground truth is **PAGE/ALTO**; tables score as **HTML-via-TEDS**
  (already in `eval_metrics.py`); KIE uses JSON. No benchmark feeds raw HTML+CSS or IDML to a text
  LLM — so HTML stays an intermediate, IDML is skipped, TXT is fallback.
- **Grobid**: scholarly-PDF specialist emitting TEI — wrong fit for scan-heavy archaeological
  reports as the general route; keep as a bake-off candidate for the digital-born scholarly slice
  only (plan §4).
- **Page-classification routing** decides *treatment* per page (extract vs OCR vs table-parser), not
  output format — output stays annotated MD (plan §4).
- **FAIR roadmap layer** (`SKILL.md`/`INDEX.md` agentic navigation) is the DR's end-state —
  **deferred** until #22 proves the basic MD pipeline on in-domain scans (plan §3.5).

## 🛠️ Landed (issue #10 §7, 2026-07-22)
- **Cue-enrich `xml_to_md.py`** — ✅ `--format layout` gives TEITOK/ALTO-derived MD the same cue
  vocabulary as the #10 converter (annotated MD unified across all three source families).
- **Citation contract (first step)** — ✅ the doc-level schema returns a `page` field from the
  nearest page marker; the explicit `[Source: <doc_id>, Page N]` string + verifier are still ahead.

## ⏳ Open / next
- **PoC** — Docling (MIT; HF-download blocked in this sandbox — run where reachable) and Marker
  (license-flagged) vs the landed pdfplumber path on the hardest PDFs, incl. `<!-- Page N -->`
  round-trip fidelity and LaTeX handling (plan §5).
- **Format bake-off via #22** — `sample_stratify.py` → candidate converters → `eval_metrics.py`
  (CER/WER/entity-F1/TEDS) + a **token-cost column** through `bench_compare.py`; decide per quality
  tier and post the verdict back to #11/#10.
- **Citation contract (finish)** — the `[Source: <doc_id>, Page N]` string + verifier once
  roadmap/agent work starts; not before.

---
_Digest rewritten in house style 2026-07-22 against `test` HEAD `fdf3d74` (previous content = the
first DR report verbatim, preserved in git history and as an issue attachment). Companion:
[`../plans/11.plan.md`](../plans/11.plan.md)._
_Refreshed 2026-09-24 against `test` `08dff48`: milestone, the 07-23 and 08-02 comments, the state of the 08-02 validation task._
