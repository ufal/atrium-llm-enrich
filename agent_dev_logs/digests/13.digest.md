# 📓 Digest — [\#13](https://github.com/ufal/atrium-llm-enrich/issues/13) `open`: The intermediate steps — data format to use

> _Condensed from `../issues/2026-07-23.13.issue.open.md` plus the four review comments on the issue (2026-07-24/26), and the current code of all six ATRIUM repos. Full strategy + specs: `../plans/13.plan.md`._
> _Companions: input-ingestion [#10](https://github.com/ufal/atrium-llm-enrich/issues/10) (`10.digest.md`, implemented) · format-decision [#11](https://github.com/ufal/atrium-llm-enrich/issues/11) (`11.digest.md`, decided) · hub DU benchmark ufal/atrium-project#22 · orchestration ufal/atrium-project#18._

**Labels**: `enhancement`, `question`, `development`, `a big one`, `integration` · **Type**: Feature
· **Milestone**: Q3 [WP8/WP7] · **Assignees**: david-spacil, motyc, rharasim, K4TEL
**Status**: 🛠️ **Decided, draft integration landed across ecosystem.** The three-plane contract and both specs stand; four review rounds with **motyc** reshaped the JSON plane's *production model* into the **"paradata pair"** accretion model (plan §G). The shared template is hub-canonical and draft integrations have landed in all five tool repositories. Minor drift and wiring issues remain.

## 🎯 Goal
Turn the issue's three candidate formats — **MD+HTML** (LLM input), **TEITOK.XML** (visually-correct layout), **JSON** (search / metadata / additional info) — into one coherent, FAIR architecture, and specify the two open items: **how detailed the HTML fragments are** and **the JSON schema template**. Neither a data-formats design doc nor a standalone JSON schema existed in the ecosystem before this.

## 🧵 Thread trail
- **2026-07-23, K4TEL** — opened with the three formats + the two "to be specified" items; linked #10, #11, hub #22.
- **2026-07-24 (repo)** — research pass over all six repos; three-plane strategy posted as a "draft to work with".
- **2026-07-24, motyc ①** — cautious about the aggregated JSON: it would make the pipeline "only work as a full run", and any partial update would force full regeneration. Separately: **no references to page thumbnails/images** — they are temporary, not stored; only originals or persistent step outputs may be referenced.
- **2026-07-24, motyc ②** — *"not a full picture. The tools will be separate and they won't see all the actual outputs of individual steps."* An aggregate needs its own tooling + running service.
- **2026-07-24, motyc ③** — **the redirect**: *"Instead of new separate tool … keep this as **paradata pair** … produce an updated json file based on the input + original (previous version) of the json file passed to the tool … better for granularity and backward compatibility … If no json is given, the tool will produce just its own part."*
- **2026-07-25/26 (repo)** — paradata-pair adopted; template + docs built in the hub (`atrium-project`). Draft integrations rolled out to `atrium-llm-enrich`, `atrium-alto-postprocess`, `atrium-nlp-enrich`, `atrium-page-classification`, and `atrium-translator`.

## 🧭 The answer in one picture
**Three planes, one job each**, bound by paradata `run_id`:
- **Reading — annotated Markdown** (`# doc` + `## Page N` + `<!-- cues -->`): the LLM diet, **derived & disposable**, regenerated from the layout plane. `pdf_to_md`/`docx_to_md` · `xml_to_md --format layout`.
- **Layout — TEITOK.XML** (TEI + facsimile + bbox): the **spatial source of truth**; human view, CQP/XPath search, correction, archival. `nlp-enrich/api_util/teitok_alto.py`.
- **Search — JSON document record**: the **canonical semantic record** for indexing / ARIADNE-GoTriple export, harmonising the existing enrichment + paradata + entity schemas.
- Markdown regenerates from TEITOK; TEITOK persists; the JSON accretes across tools. Nothing circular, and **no transient artifact is ever referenced**.

## 🔎 Spec 1 — HTML-fragment detail: **full `CUE_SCHEMA` by default**
Markdown is a throwaway view, so emit **every** recoverable cue as inert HTML comments (`layout_md.CUE_SCHEMA`: 15 comment cues + 8 inline GFM forms). Kept faithful, not bloated, by two invariants: **aggregate at block level** (word/line coords stay in TEITOK) and **cues are positional hints, never content**. `standard`/`minimal` down-profiles stay selectable via a future `--detail` knob; the token-cost check rides with #22. Several cues remain **defined-but-reserved** (`weight`, `INDENT`, `LAYOUT_COLUMN`, `LAYOUT_MARGIN`, `STYLE` line-spacing, `WATERMARK`).

## 🔎 Spec 2 — the JSON record, built as a **paradata pair**
One canonical, versioned, FAIR record per document — and, per motyc's redirect, **not** produced by a central service. It travels *with* the document: each tool takes the previous version, if given one, and returns it with **only its own block** updated.


```
doc.json ──► [alto] ──► doc.json ──► [nlp] ──► doc.json ──► [llm] ──► doc.json
pages/lines           entities            enrichment
```

**Six rules:** ① optional `--document-json` in / out · ② **own block only**, everything else passes through untouched · ③ **no baseline → own part only** (standalone-safe) · ④ each block stamped with its writer's `program`/`run_id`/`paradata_ref` (granularity) · ⑤ licenses accrete via `para_licenses.merge_effective_licenses` · ⑥ unknown/newer blocks preserved, newer major refused.

**Ownership** is one-per-block, with field-level splits where several tools contribute (`pages`, `lines`, `entities`), so nothing is co-mutated.

**Reference discipline:** only the **original input** (keyed `doc_id` + `sha256`) and **persistent step outputs** (`derived_from`). Markdown and page images are *disposable*, so they appear in `regenerable` as a recipe — never as a stored path.

**Why it fits:** `atrium_paradata.py` already has the round-trip primitive (`_to_state_dict`/`_from_state_dict`, the `load_paradata` version guard, `merge_run_paradata`), and ~17 `ParadataLogger` sites across the five tools are exactly the seams the paired hook attaches to.

## 🛠️ Landed
- **Hub** (`atrium-project`): `docs/templates/shared/atrium_document.py` + `atrium_document.schema.json` + `docs/document_schema.md` + an example fixture. Hub-canonical under `para-drift` GHA checks (newly added). Public `get_license_block()` accessor added to `atrium_paradata.py`.
- **Ecosystem Rollout**: Draft implementations landed in `atrium-llm-enrich`, `atrium-alto-postprocess`, `atrium-nlp-enrich`, `atrium-page-classification`, and `atrium-translator`. 
- **Bugfixes**: `alto-postprocess` fixed hardcoded legacy program names (`langID-classify` / `langID-aggregate`) by adopting a shared `PROGRAM_NAME` in `document_hook.py`.

## ⏳ Open / next
- **Fix `para-drift`**: The hub's `atrium_document.py` was reformatted today (cosmetic wrapping + stale doc-path comment removal), causing `atrium-llm-enrich` to drift. Needs syncing.
- **Wire up `get_license_block()`**: The accessor exists in `atrium_paradata.py` now, but call sites (`llm-enrich`'s `ollama_client.py`/`openrouter_client.py` and `translator`'s `main.py`) are still calling the private `_logger._license_block()`.
- **Fix stale docstring**: `alto-postprocess/document_hook.py` still contains a docstring claiming program names are hardcoded, even though the code was just fixed.
- **Finish `llm-enrich` integration**: `llm_run.py` and `service/` remain untouched for the document schema integration.
- Reserved-cue emitters + the `--detail` knob; the #22 token-cost bake-off; unifying nlp-enrich onto MD input.
