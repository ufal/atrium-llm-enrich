# Strategy — Issue #25: Evaluate `archivatorium` as a VLM OCR backend

> Issue [#25](https://github.com/ufal/atrium-llm-enrich/issues/25) · Labels: `enhancement`, `question`,
> `development`, `integration` · Milestone: WP4/T4.1 (stage 5) + WP3/T3.3 — Final release, architecture frozen ·
> **Status: 🆕 opened 2026-09-17, no discussion; blocked on a licence before any code.**
> _Written 2026-09-24 against `test` `08dff48`. Evidence and context: [`../digests/25.digest.md`](../digests/25.digest.md)._

## Context
The issue proposes one more engine for #10's scanned / curve-only PDF path and names the seam itself. It is a
text-only engine (Markdown out, no coordinates), from a partner in the LINDAT/CLARIN family, with no licence in its
repository. #10's decision (2026-09-24) leaves engine choice to the #22 benchmark, where #24 (olmOCR) already waits.

## Status snapshot
| Item                                   | State           | Note                                                                           |
|----------------------------------------|-----------------|--------------------------------------------------------------------------------|
| Seam named                             | ✅ **DONE**      | #10 render+OCR path (`pdf_to_md.py --ocr`), per `plans/24.plan.md` §B          |
| Licence of archivatorium               | ❌ **BLOCKER**   | no `LICENSE` file (per the issue); nothing vendored or shipped until there is  |
| Weight licences (`glm-ocr`, `firered`) | ❓ **UNKNOWN**   | ask with the licence question                                                  |
| `--ocr-engine` switch in `pdf_to_md`   | ❌ **NOT BUILT** | only `--ocr`, `--ocr-lang`, `--ocr-dpi` exist                                  |
| Scoring harness                        | ✅ **EXISTS**    | `bench_compare.py` + `eval_metrics.py` + `sample_stratify.py`                  |
| Test pages                             | ⚠️ **PARTLY**   | `curves.pdf` / `scan.pdf` are not in the repo; needs committed or shared pages |
| Scores for any VLM candidate           | ❌ **NONE**      | Tesseract `ces` is the only engine that has run                                |

## Strategic actions
### A — Licence first (external, cheap)
Write to Jan Švec: licence (MIT/Apache-2.0), current state, interest in a joint evaluation (our harness and
archaeological gold pages against theirs), and the origin and licences of the Ollama model tags. Record the answer in
`para_config.txt` terms before anything is wrapped.

### B — Wrap, don't import
Behind the existing seam: `pdf_to_md.py --ocr-engine {tesseract,archivatorium}` calling its CLI in a subprocess
(its Python 3.12 requirement and `PyPDF2`/`pdf2image` stay out of our environment), with the `NEEDS_OCR` fallback
unchanged on failure and model, mode and sampling parameters written to paradata. No new entry point.

### C — Score, then decide
`bench_compare.py` on the same pages: Tesseract `ces` against all four modes, CER/WER per quality tier, plus an
on/off ablation of previous-page context. Report the table to hub #22 and decide adopt / benchmark-only / drop.

### D — If adopted: where the text goes
A VLM transcription is OCR output: its record origin must be a `vlm:…` value, which `ORIGIN_ORIGINATORS` hands to
alto-postprocess, and it has no boxes — so it fills `content`/`lines[].text` but never `bbox`, and a TEITOK file made
from it has no facsimile. Say so wherever the engine is offered.

## Verification / done-criteria
- A licence answer on record (issue comment + `para_config.txt` row if adopted).
- The `bench_compare.py` report with five columns (Tesseract + four modes) on one page set, and the ablation.
- A recommendation paragraph on the issue and a line in hub #22.

## Out of scope
- `clean` / `metadata` / `interlink` (they duplicate nlp-enrich and the vocabulary mapping).
- Any geometry: none comes out of this engine.
