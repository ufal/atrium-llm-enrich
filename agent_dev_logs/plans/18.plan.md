# Strategy — `ufal/atrium-llm-enrich` (Issue #18: Build explicit PDF/DOCX-2-JSON converter - digital-born documents)

> Issue [#18](https://github.com/ufal/atrium-llm-enrich/issues/18) ("Build explicit `PDF`/`DOCX`-2-`JSON` converter - digital-born documents") · Labels: `development`, `a big one`, `integration`
> **Status: 🔓 Architecture defined, §1a resolved and hardened — `atrium_document.schema.json` is the sole canonical target. Refreshed 2026-08-04 (review pass): §0b drift is CLOSED, §1c is IMPLEMENTED, the font row and the `para_config.txt` mapping are DECIDED, and the §1a contract itself was found escapable in five ways and fixed (see §0c). Suite: 382 passed, 6 skipped, 0 failed (as CI runs it). PR 2/3 are unblocked — PR 0.5 now needs only the `v1` tag move.**

## §0c — Review pass, 2026-08-04: what changed

The previous refresh's own lesson ("a '✅ landed' claim needed auditing against the repo
rather than trusting") applied to this file again. Corrections, each verified against the
tree rather than inferred:

* **§0b drift is closed.** `diff` on both pairs is empty; `scripts/revendor_shared.sh --check`
  reports clean for all seven enforced files. `docs/document_schema.md`'s ownership table,
  Contract paragraph, `tables`/`forms` entries and the versioning-rule-2 amendment are all
  present. PR 0.5's re-vendor and docs halves are **done**; only the `v1` tag move is not.
* **§1c is implemented, not merely specified.** It was recorded as "Fixed" while
  `_rows_from_lines` still built `{page_num, line_num, text}` and dropped everything else,
  and while the schema's `group_id` description named `rows_to_layout_markdown()` as the
  implementing function — which had no group tracking in it. Both sides now exist, and
  `tests/test_json_to_md.py` pins the blank line **and** pins that output is byte-identical
  when no row carries a `group_id` (the whole ALTO path).
* **The §1a contract was escapable in five ways.** All five are fixed and tested; the
  write-ups are in `docs/document_schema.md`'s "Issue #18 review pass" changelog. In short:
  a block written before `set_source()` escaped the check permanently; `merge_document_records()`
  overwrote `source` last-path-wins and dropped `derived_from`; `own_fields` conferred
  writership on any program; origin matching was case-sensitive with no bare `pdf`, and a
  non-match abstains, so `DOCX`/`pdf`/`abbyy-alto` silently switched §1a off; and `tables`
  was declared originatable but had neither key fields nor field owners, so `merge_block()`
  either raised or emptied every row down to its key.
* **The digital→OCR hand-off was unreachable.** `needs_ocr` is granted to `digital-convert`
  so it can request OCR re-acquisition, but the frozen `digital-born-*` origin then refused
  every write `alto-postprocess` attempted — §3 contradicted §1a. A record whose own `pages[]`
  sets `needs_ocr: true` now authorises the re-origination.
* **The licence decision was inert.** `para_config.txt` had no `[components]` rows, so
  `log_component("docling")` stored `UNKNOWN` and `para_licenses` treats `UNKNOWN` as
  maximally restrictive: a digital-born record came out with `provenance.license = "UNKNOWN"`
  regardless of the stack. Worse than the CC BY-NC 4.0 default, and the exact opposite of the
  permissive outcome the pdfplumber-over-PyMuPDF argument exists to secure. Rows added;
  measured: `provenance.license` is now `MIT`.
* **`requirements_digital.txt` contradicted itself.** It listed `pymupdf` as an *active*
  dependency five lines above a commented-out copy of itself marked "DO NOT UNCOMMENT
  WITHOUT THE REVIEW BELOW". The AGPL trap this plan, the digest and
  `digital_born/README.md` all describe as avoided was still declared.
* **The A/B protocol was not runnable.** `digital_born/README.md` named
  `probe_pdf_geometry.py` and invoked it with `--engine`/`--compare`; the file was
  `probe_pymupdf.py` and had no flags. The harness now exists, emits `$defs/bbox`'s
  convention directly, and measures the inter-line gaps the block-boundary claim rests on
  (2.0 pt within a paragraph vs 34.0 pt between, `separable: true`).
* **The fixtures were not reproducible and not checked.** `MANIFEST.json` was committed one
  directory above where the generator writes it, so running the generator produced a second
  manifest and left the committed one permanently stale — and `--verify` never read a
  manifest at all, only comparing regenerated bytes against on-disk bytes that (the fixtures
  being uncommitted) were never there. The DOCX also embedded `ZipInfo.create_system`, which
  is `0` on Windows and `3` elsewhere, and used DEFLATE, whose output depends on the linked
  zlib — so the pinned sha256 was platform- and build-specific. Fixed (`ZIP_STORED`, pinned
  `create_system`), manifest moved beside the fixtures and regenerated, and
  `tests/test_digital_fixtures.py` now runs the generator, checks the hashes, and verifies
  the PDFs' xref arithmetic and that the DOCX opens in python-docx.
* **Nothing could locate the schema.** Plan §2's Layer D makes validation the output gate,
  but the hub keeps the schema under `docs/templates/shared/` and tool repos at their root,
  and the only locator anywhere was a relative walk inside one test — which was itself
  `pytest.importorskip("jsonschema")`-guarded, and `jsonschema` was in no requirements file,
  so the single schema check in the repo silently skipped on a clean venv.
  `schema_path()`/`load_schema()`/`validate_document()` added; `jsonschema` declared.

Still open and deliberately untouched: PRs 2–7 (the adapters, the entrypoint, migration) and
the `v1` tag move.

## Context

* Issue #18 requires bridging digital-born documents (PDF and DOCX) into the LLM semantic enrichment workflow.
* Previously (via Issue #10), the pipeline relied on a PDF/DOCX → Markdown conversion, utilizing HTML comments (`<!-- BBOX -->`, `<!-- PAGE_BREAK -->`) to preserve spatial cues.
* This strategy explicitly rejects rewriting the Markdown pipeline. Instead, we are treating Issue #18 as an **architecture-first conversion task**.
* The pipeline will transition from a paradata-pair accretion model to utilizing `atrium_document` as the first-class intermediate representation (IR). Markdown is no longer the IR.
* **What changed since the first draft:** PR 0 was never a TODO — `digital_born/probe_docling.py` and siblings already exist and had been run against `sample.pdf`, producing real Docling IR (`sample.pdf_docling_ir.json`, 197 KB). The same change relocated the legacy `api_util/*_to_md.py` converters and broke three test modules; that is §0, now fixed.
* **What changed on the second pass (2026-08-03):** implementing the §1a decision surfaced that the *stated* blocker was only part of the problem. The plan's own "landed ✅" claims needed auditing against the repo rather than trusted — see §0b in particular, where a change recorded as landed had in fact landed in one of two byte-identity-enforced copies.

## §0 — Fix the in-progress regression first ✅ done

* **Broken imports** — resolved by restoring `pdf_to_md.py` / `docx_to_md.py` / `doc_to_visual_md.py` to `api_util/`, leaving `digital_born/` as pure Phase-0 scratch space. PR 7 remains the actual removal point.
* **Stale dependency manifest** — `requirements_docmd.txt` annotated as the legacy MD-era stack; new `requirements_digital.txt` declares the digital-born stack.
* Full suite: **382 passed, 6 skipped, 0 failed** (2026-08-04, after the §0c review pass, with `requirements-test.txt` installed as CI now does; was 304/6 at the time of this entry).

### §0b — ✅ CLOSED (2026-08-04): the §1 change had landed in the hub copy only

`para-drift.reusable.yml` contains these steps, and a caller in every tool repo:

```yaml
- name: Verify atrium_document.py parity
  run: diff -u tool-repo/atrium_document.py hub-repo/docs/templates/shared/atrium_document.py
- name: Verify atrium_document.schema.json parity
  run: diff -u tool-repo/atrium_document.schema.json hub-repo/docs/templates/shared/atrium_document.schema.json
```

The two pairs **no longer** differ — `diff` is empty for both and `scripts/revendor_shared.sh --check` reports clean for all seven enforced files. The table below is the state as found on 2026-08-03, kept because the failure mode is worth recognising again:

|                         | `docs/templates/shared/` (hub) | `atrium_document.*` (llm-enrich root) |
|-------------------------|--------------------------------|---------------------------------------|
| `lines[].group_id`      | ✅                              | ❌                                     |
| `tables` block          | ✅                              | ❌                                     |
| `forms` block           | ✅                              | ❌                                     |
| the digital field grant | ✅ (`llm-enrich-digital`)       | ❌                                     |
| Issue #18 inline note   | ✅                              | ❌                                     |

Two consequences, both needing action:

1. **This is either red now or red the instant `v1` moves.** The check reads the hub side at its `hub-ref` input, default `v1` — a moving major tag only a maintainer advances. ~~Per #13's own TODO list, no repo runs `pytest` in CI, only lint/format, so "304 passed" is a local claim and para-drift is the *only* CI gate that would catch this.~~ **That clause is wrong** and is struck rather than deleted, because believing it is what made a local-only green suite feel sufficient: `docker.yml` -> `docker-tool.reusable.yml@v1` runs the full suite with coverage on every push. See §5.
2. **`tables` and `forms` were added to the hub schema *and* module with no plan, digest, or `docs/document_schema.md` entry.** That doc's ownership table still lists five tools and neither block, and its "Contract" paragraph still enumerates the old block set. Fix all three in the same pass.

Correct the digest wording too: "✅ Landed in `atrium_document.schema.json` + `atrium_document.py`" should read *landed in the hub canonical only; five vendored copies and a `v1` move pending.* Use `scripts/revendor_shared.sh --check` before claiming parity.

## §1 — The Mapping Contract ✅ decided

* **Decision (2026-08-03)**: extend the schema rather than accept a fidelity loss. `lines[]` items gain a `group_id` field (`string | null`) — lines sharing a `group_id` are contiguous, same-reading-order, and came from the same DOCX paragraph/table row or PDF text block; `null` for OCR/ALTO lines from alto-postprocess, which stays line-only. Deliberately kept as a field on `lines` rather than a new nesting level, so `bbox` and the `entities` block's `page`/`line` keys don't have to learn a second addressing scheme.

### §1a — Block ownership for digital-born docs ✅ RESOLVED

**The question.** `BLOCK_OWNERS` hardcoded `pages`/`content`/`lines`/`tables` as owned by `alto-postprocess`, on the assumption that ALTO/OCR always originates a document's positional data first. A digital-born PDF/DOCX never runs alto-postprocess, so no `program` identity was authorized to originate them.

**The decision.** Neither of the two options originally tabled. `BLOCK_OWNERS` conflates two different things — *who is authoritative for a block's semantics* and *who happens to originate it first* — and for the positional plane those differ. The second question already has an answer in the record: **`source.origin`**, which is already first-writer-wins in `set_source()` and already carries the right examples (`digital-born-pdf`, `docx`, `ocr:pero`, `ABBYY-ALTO`).

So: **one originator *role* per block, a declared candidate tuple, and the choice fixed per document by `source.origin`.**

```python
BLOCK_OWNERS: Dict[str, Union[str, Tuple[str, ...]]] = {
    "pages":   ("alto-postprocess", "digital-convert"),
    "content": ("alto-postprocess", "digital-convert"),
    "lines":   ("alto-postprocess", "digital-convert"),
    "tables":  ("alto-postprocess", "digital-convert"),
    ...
}

ORIGIN_ORIGINATORS: Tuple[Tuple[str, str], ...] = (
    ("digital-born", "digital-convert"),
    ("docx",         "digital-convert"),
    ("ABBYY-ALTO",   "alto-postprocess"),
    ("ocr:",         "alto-postprocess"),
    ("vlm:",         "alto-postprocess"),
)
```

`_assert_origin_consistent()` enforces it, and is **self-guarding** — a no-op for single-owner blocks, for non-candidate programs (nlp-enrich merging morphology into `lines[]` is a field contribution, not an origination claim), for records with no `source` yet (rule 3), and for origins the table hasn't been taught (abstain, don't block). So it is additive for every pre-#18 caller.

It is also called from **`merge_block()`**, which previously bypassed `_assert_owner()` entirely — that is why `pages` and `lines` were never ownership-checked at all, the half of §1a the first write-up missed.

**Why the two tabled options were rejected** (recorded so this isn't relitigated):

1. **No-op alto passthrough.** Stamps `assembled.blocks[<block>].program = "alto-postprocess"` and appends a `provenance.contributors[]` entry with a `paradata_ref`, for a run that did nothing, in a record whose purpose is FAIR catalogue export. Rule 4 exists for attribution granularity; this falsifies it. It also puts the alto container back on the digital-born critical path, which the four-layer design exists to avoid.
2. **A second name in `BLOCK_OWNERS` with no further guard.** `content` and `tables` are not field-split, so either originator could `set_block()` straight over the other's. The mutual exclusivity that makes two originators safe would be an unwritten assumption instead of a check.

**Why `source.origin` and not the `assembled.blocks` stamp:** rule 4 says the stamp on a field-split block names the **most recent** writer. Once nlp-enrich merges into `lines[]`, the originator signal is gone. `source` is immutable after first write.

**No `SCHEMA_VERSION` bump.** A literal reading of `docs/document_schema.md`'s versioning rule 2 ("changing block ownership bumps to the next major") would force a `2.0` migration across five repos. It shouldn't: no field is renamed or removed, every existing record stays valid, every existing tool keeps every capability, and `BLOCK_OWNERS` is a *write-authorisation* table whose read-time counterpart (`assembled.blocks[<block>].program`) is unchanged. Rule 2 is amended to say so explicitly — **widening write authorisation is additive; re-attributing an already-written block would not be** — because leaving it implicit invites either an unnecessary migration or a silent contract break.

### §1b — ✅ ADDRESSED: as first landed, §1 silently dropped all text and bbox

`BLOCK_FIELD_OWNERS["lines"]["llm-enrich-digital"] = ["group_id"]`. Trace `merge_block()`:

```python
allowed  = ["group_id"]
writable = {"group_id"} | {"page", "line"}
patch    = {f: v for f, v in incoming.items() if f in writable}   # ← silent
```

`text` and `bbox` are discarded **with no warning** — `_complain()` only fires when `allowed is None`, and this grant is not `None`. Then `lines.items.required` is `["page", "line"]` only with `additionalProperties: true`, so the record **passes** `jsonschema.validate()`. §2's "validation is a runtime gatekeeper" does not gate this class of defect at all.

It surfaces three stages later: `json_to_md._rows_from_lines` skips empty-text lines, `rows` comes back empty, `content` can't be written either, and you finally get `ValueError("nothing to render")` pointing at the wrong thing.

Also: **`lines[].bbox` was granted to `nlp-enrich`, not `alto-postprocess`** — so the PDF adapter, whose entire selling point per §3 is exact native coordinates, was not authorized to write the field that carries them.

Corrected grants:

```python
"pages": {
    # ... existing owners unchanged ...
    "digital-convert": ["page_index", "canvas", "quality_score", "quality_band", "needs_ocr"],
},
"lines": {
    "alto-postprocess": ["categ", "quality_score", "lang", "text"],
    "nlp-enrich": ["lemma", "upos", "feats", "teitok_ref", "bbox"],
    "digital-convert": ["text", "bbox", "group_id", "lang", "quality_score", "categ"],
},
```

`bbox` is granted twice deliberately — on the ALTO path nlp-enrich derives it while aligning to TEITOK; on the digital-born path there is no TEITOK and the adapter's native coordinates are the only bbox the record will ever have. The two never meet on one document, and that is now enforced rather than assumed.

**Two mitigations, at different scopes:**

* **Local (in scope for #18).** A round-trip assertion in Layer D, before `validate()`:

  ```python
  written = {(r["page"], r["line"]): r for r in doc.get_block("lines") or []}
  for row in rows:
      got = written.get((row["page"], row["line"]), {})
      lost = [f for f in ("text", "bbox", "group_id") if f in row and f not in got]
      if lost:
          raise RuntimeError(
              f"line {row['page']}/{row['line']}: {lost} dropped by merge_block — "
              f"{PROGRAM!r} is not declared for them in BLOCK_FIELD_OWNERS['lines']"
          )
  ```

* **Ecosystem — ✅ now available opt-in, no call-site cleanup needed.** `merge_block()` records every dropped field; `dropped_fields()` exposes them, `assert_fields_survived()` raises on them, and `warn_dropped_fields=True` turns the warning on per record. The deferral rested on a call site that does not exist in this repo (see the note in `tests/test_document_originators.py`), so an opt-in flag gets the guarantee without the tightening pass:

  ```python
  dropped = {f for r in records for f in r} - writable
  if dropped:
      self._complain(f"{self.program!r} may not write {sorted(dropped)} in block {name!r} — dropped. "
                     f"Declare them in BLOCK_FIELD_OWNERS or pass own_fields=[...]")
  ```

  This makes silent field loss impossible for all five tools and would have caught §1b immediately — but it is **not free**. At least one existing call site passes context fields it doesn't own for readability (`tests/test_atrium_document.py` has the translator merging `entities` with `surface`, which translator doesn't own), so it would start warning and would raise under `strict=True`. A deliberate tightening pass with call-site cleanup, on its own.

### §1c — ✅ IMPLEMENTED (2026-08-04): `group_id` was inert until a consumer read it

`api_util/json_to_md.py` turns the record back into the LLM's diet. `_rows_from_lines` builds `{page_num, line_num, text}` plus an optional `bbox` and **drops everything else**; `_pages_meta` reads only `canvas`/`needs_ocr`/`ocr`. So the paragraph fidelity §1 bought never reaches the model. A schema field with no consumer is a no-op with extra steps.

The fix is smaller than expected and worth recording as a design point: **Markdown already encodes paragraphs natively as a blank line**, so a `group_id` boundary needs *no new `layout_md` cue* — it maps onto the format's own primitive rather than onto another `<!-- … -->` comment. `rows_to_layout_markdown` now tracks `current_group` and appends `""` on a change; with `group_id` absent everywhere (the ALTO path) the output is byte-identical, and `test_absent_group_id_is_byte_identical_to_before` asserts the exact string. A distinct sentinel is used rather than `None`, since `None` is itself a legitimate `group_id` and the first text row of a page must never emit a boundary.

`tables` rendering is the remainder: the block carries grid **shape** only, so emitting it means joining `tables[].cells` back to line text through `group_id` and feeding `layout_md`'s existing `md_table` emitter. Deliberately not stubbed — a half-rendered table is worse for the model than the grouped lines it already gets.

Two smaller finds in the same file:

* ✅ **Fixed.** `_rows_from_lines` did `int(line["page"])` and `continue`d on failure while the schema keeps `page` a string *"so 'iv' or 'A-1' survive"*, so non-numeric labels vanished with no diagnostic and `convert()` then died with `ValueError("nothing to render")` blaming upstream stages for text they had produced. The renderer still needs an int key, so `page_ordinals()` derives one: `int(label)` when every label is numeric (the ALTO path, unchanged), else `pages[].page_index`, else document order — and the real label is carried through as `page_label` for the `## Page …` heading. Note this is *not* solvable by telling the converter to emit numeric labels: roman-numeral front matter is exactly the archival material #18 exists to ingest.
* `DROP_CATEGORIES = {"Garbage", "Inverted"}` — §2's Layer B must emit **exactly these two `categ` values** rather than inventing new ones. That reuses the existing filter and routes Issue #10's diacritic-corruption finding into the JSON path for free. Now stated in the schema's `lines[].categ` description too, since `categ` is an open string: a synonym does not fail validation, it silently disables the filter.

### §1d — ✅ DONE: program identity `llm-enrich-digital` → `digital-convert`

Every other identity in these tables is a **role** (`alto-postprocess`, `page-classification`, `nlp-enrich`), not a repo. `llm-enrich-digital` encodes the accident that this converter happens to live in `atrium-llm-enrich`, and that is not cosmetic:

* it lands in `provenance.contributors[].program` in FAIR catalogue exports — permanently;
* any consumer doing `program.startswith("llm-enrich")` conflates a pre-processing converter with the enrichment stage;
* it is simultaneously a `ParadataLogger` identity needing a `para_config.txt` component→licence mapping, so it's a two-schema commitment.

### The mapping table

| Source concept       | PDF Adapter            | DOCX Adapter            | `atrium_document` Target                                                      |
|----------------------|------------------------|-------------------------|-------------------------------------------------------------------------------|
| Document metadata    | PDF metadata           | Core properties         | `source` + document metadata                                                  |
| Page                 | PDF page               | Derived from pagination | `pages[]` item                                                                |
| Text block/paragraph | PDF text grouping      | Paragraph/table/etc.    | `lines[].group_id` (shared value across the group's lines)                    |
| Text line            | PDF positioned text    | Paragraph/run-derived   | `lines[]` item                                                                |
| Text                 | PDF text span          | DOCX run                | `lines[].text`                                                                |
| Bounding box         | Native PDF coordinates | **Omitted** (§3)        | `lines[].bbox` — origin **top-left, y down**, unit from `pages[].canvas.unit` |
| Table grid           | Detected table         | `w:tbl`                 | `tables[]` item; each cell linked to its lines via `cells[].group_id`         |
| Font family / size   | PDF font               | DOCX run                | ✅ `lines[].style` *{bold, italic, heading_level}* — semantic style only (option (c)) |
| Page break           | Page boundary          | Explicit/derived break  | `pages[]` boundary                                                            |
| Undecodable text     | no `/ToUnicode`        | n/a                     | `pages[].needs_ocr` + `needs_ocr_reason` + `lines[].categ = "Garbage"`        |

**The font row is DECIDED (2026-08-04): option (c).** `lines[].style` carries `{bold, italic, heading_level}` and nothing else — no typeface, no point size. A downstream reader can act on "this was a heading"; it cannot act on "this was Helvetica 12pt", and `categ` already partly carries heading-ness. python-docx (`w:b`, `w:i`, `outlineLvl`) and pdfplumber (`fontname`, `size`) both supply what is needed. Granted to `digital-convert` only; absent on the ALTO path, which has no style signal — the same additive shape as `group_id`. The two rejected options, recorded: (a) accept the loss, which discards a real signal for free; (b) `lines[].font` with `{family, size, …}`, which stores what no consumer can use.

## §2 — Four-Layer Architecture Design

* **Layer A — Format Extraction (`api_util/digital/`)**: **Docling** as the primary structure foundation across formats; a **PDF geometry** library for exact coordinates (see the licence decision below); **python-docx / docx2python** as OOXML fallback where Docling drops Word metadata. `pdf.py` and `docx.py` extract into a shared internal `DigitalDocument`.
* **Layer B — Layout Normalization**: normalizes coordinates, reading orders and groups; applies quality-score heuristics (vowel/consonant ratio, dictionary hit-rate, perplexity gates) to filter garbage text and inverted fonts. **Emit `categ` ∈ `{"Garbage", "Inverted"}`** so `json_to_md`'s existing `DROP_CATEGORIES` filter applies unchanged.
* **Layer C — Serialization**: converts `DigitalDocument` into a strict `AtriumDocument` dictionary via `DocumentRecord`, as `program="digital-convert"`, calling `set_source(origin="digital-born-pdf" | "docx")` **before** any block write, since that is what authorizes them (§1a).
* **Layer D — Validation & Export (`api_util/digital_to_json.py`)**: strict schema validation. **Rule:** if `jsonschema.validate()` fails, no `doc.json` is emitted. **Plus** the §1b round-trip assertion, because the schema cannot catch a field-ownership drop.

### PDF geometry: a licence decision, not just a technical one 🔴

Rule 5 merges component licences as a **most-restrictive union** into `provenance.license` on every record. Declaring **PyMuPDF (AGPL-3.0)** as a component of a `digital-convert` run sets `provenance.license = AGPL-3.0` for **every digital-born document**, and that field goes to catalogue export. The blast radius is the licence on the output data for an entire input class, not one dependency line. Issue #10's research pass already flagged PyMuPDF under "licence traps" against an otherwise permissive-first stack.

**The escape looks cheap.** Docling already owns structural grouping, so PyMuPDF's unique contribution is only precise span geometry. Measured against `tests/fixtures/digital/minimal.pdf`, **pdfplumber** (MIT, already vendored for the docmd stack) returns char-level geometry to three decimals (`x0=72.000 x1=80.004 top=62.484 bottom=74.484`), plus `fontname`, `size` and the transformation matrix, with inter-line gaps that make block boundaries detectable (2.0 pt within a paragraph vs 34.0 pt between). That is every field `lines[].bbox` needs plus the font data the mapping table wants.

**Run the A/B on `sample.pdf` before PR 3** (protocol in `digital_born/README.md`), not on the minimal fixture — the fixture is single-rotation Helvetica, exactly the easy case where the two agree. Decide on the numbers and record it in this section either way.

Two things this needed before it could be more than an argument, both now done: the harness (`digital_born/probe_pdf_geometry.py --engine/--compare`, which the README already documented but which did not exist), and the `para_config.txt` `[components]` rows without which every digital-born record reported `provenance.license = "UNKNOWN"` — treated as maximally restrictive — no matter which library won. Measured on the fixture, pdfplumber's inter-line gaps are 2.0 pt within a paragraph vs 34.0 pt between (`separable: true`), so PyMuPDF's block API has no unique contribution there either.

## §3 — Format-Specific Fidelity Rules

* **PDF (Spatial Priority)**: prioritize exact character/span coordinates, strict reading orders and native bounding boxes. Mixed PDF processing routes per page on digital-text-layer detection before deferring to OCR — expressed in the record as `pages[].needs_ocr`.
* **DOCX (Structural Priority)**: preserve paragraphs, heading styles, explicit page breaks and tables. **Do not fabricate bounding boxes** where renderer coordinates are unavailable — `lines[].bbox` is simply absent, and that is documented rather than approximated. The tension with §1 that this rule used to have is resolved: paragraphs are preserved as `lines[].group_id`, independent of geometry.

## §4 — Implementation and PR Rollout Sequence

* **PR 0 (Prototyping)** — ✅ **Done.** `digital_born/` holds the probes + exploration inputs; the legacy converters are restored to `api_util/`; deps declared in `requirements_digital.txt` (and licence-mapped in `para_config.txt`); full suite green. The `test_*.py` → `probe_*.py` rename has **landed**, and `pytest.ini` now also carries `norecursedirs` because `testpaths` does not apply when pytest is given a path argument.
* **PR 0.5 (Re-vendor)** — ⚠️ **mostly done.** The hub edits and `docs/document_schema.md` (ownership table, Contract paragraph, `tables`/`forms`, versioning-rule amendment, changelog) are in, and this repo's two vendored copies are byte-identical. **Remaining: re-vendor into the other four tool repos and move `v1`** — the check reads the hub side at `hub-ref` (default `v1`), so nothing is enforced until the tag moves.
* **PR 1 (Contract)** — ✅ **landed and hardened.** §1a `BLOCK_OWNERS` tuples + `ORIGIN_ORIGINATORS`/`resolve_originator()` + `_assert_origin_consistent()` (now deferred, not permanently abstaining); §1b field grants plus `assert_fields_survived()`/`dropped_fields()`; §1d rename **and** its `para_config.txt` mapping; the font-field decision (option (c), `lines[].style`); `tables` key fields and field owners; `tests/test_document_originators.py` (52 tests).
* **PR 2 (Model)** — `DigitalDocument` common internal normalized representation.
* **PR 3 (PDF Adapter)** — digital-born PDF extraction (Docling + the geometry library chosen in §2).
* **PR 4 (DOCX Adapter)** — DOCX structural extraction (Docling + OOXML fallback).
* **PR 5 (Entrypoint)** — `api_util/digital_to_json.py` + CLI auto-detection + the Layer D round-trip assertion.
* **PR 5.5 (Consumer)** — ⚠️ **half done.** `json_to_md` / `xml_to_md` now consume `group_id` (blank-line paragraph boundary), non-numeric page labels, `canvas.unit` and `needs_ocr_reason`. **Remaining: `tables` → GFM**, which is now expressible because `cells[].group_id` gives the join a key; deliberately not stubbed, since a half-rendered table is worse for the model than the grouped lines it already gets.
* **PR 6 (Integration)** — `atrium-llm-enrich` compatibility pipeline tests. #13 TODO 1's "no repo runs pytest in CI" is **stale**: `docker.yml` -> `docker-tool.reusable.yml@v1` runs `pytest -m "not slow" --cov` with a 42% floor, so integration tests added here are enforced.
* **PR 7 (Migration)** — deprecate `api_util/pdf_to_md.py` / `docx_to_md.py` and delete `digital_born/`.

*Not in this sequence:* the `merge_block()` dropped-field warning + call-site cleanup (§1b), which is ecosystem-wide and should not ride on #18.

## §5 — Testing & CI/CD Strategy

* **Unit fixtures** — ✅ `tests/fixtures/digital/` generated by `make_fixtures.py`: `minimal.pdf` (1039 B, 2 pages, 3 text blocks), `garbled.pdf` (722 B, WinAnsi-over-cp1250 with no `/ToUnicode`), `minimal.docx` (4455 B — was 2366 B under DEFLATE, now `ZIP_STORED` for byte determinism; heading + 2 paragraphs + 2×2 table + explicit page break). `MANIFEST.json` sits **beside them** and `--verify` now checks it as well as any bytes on disk. The bytes themselves are not committed, so `tests/test_digital_fixtures.py` runs the generator and asserts the hashes — otherwise the pinning is advertised and unexercised. All three validated: `qpdf --check` clean, pdfplumber returns exact bboxes, python-docx reads the structure, and `garbled.pdf` reproduces the corruption (`sondě`→`sondì`, `hřeby`→`høeby`) — note `Zpráva` survives intact, since `á` shares a codepoint, so the output is *partially* correct, which is what makes it dangerous.
* **Golden tests** — compare `actual.json` vs `expected.json` with unstable metadata stripped. Reviewable precisely because the fixtures are hundreds of bytes, not megabytes.
* **Pipeline compatibility** — pass the generated JSON into `llm_run.py` in CI to guarantee no downstream schema or structural panics.
* **CI DOES run pytest** — correcting a claim repeated three times in this file and the digest. `docker.yml` calls the hub's `docker-tool.reusable.yml@v1`, which runs `python -m pytest -m "not slow" --cov=. --cov-report=term-missing --cov-report=xml` with a 42% floor from `.coveragerc`. #13 TODO 1 ("no repo runs pytest in CI, only lint/format") is **stale**, and acting on it as if it were true is what let two landing defects reach a red build: the plan treated the local suite as the only signal and para-drift as the only gate.
  What is genuinely broken is narrower and worth fixing: the caller passed `requirements: "requirements.txt requirements_remote.txt"`, so **`requirements-test.txt` was never installed in the job that runs the tests**. The reusable's own `pip install pytest pytest-cov ruff` was the only reason pytest worked, every runtime dep a test needs had to be duplicated into `requirements.txt` (that is why `pandas`/`pydantic`/`tqdm` are there), and anything declared only in `requirements-test.txt` silently did not exist — `tests/test_api_contract.py` module-skipped on missing `fastapi`, and the schema tests failed on missing `jsonschema`. Fixed by adding the file to the caller's list; it un-skips 5 tests, all passing.
* **Drift** — `scripts/revendor_shared.sh --check` locally before pushing; para-drift in CI. Remember it reads the hub at `hub-ref` (default `v1`), not at your merge branch. `--all` used to exit 0 having checked nothing when its glob matched no repos; it now exits 2.
* **Landing discipline** — this change contains a **file move** (`tests/fixtures/MANIFEST.json` -> `tests/fixtures/digital/MANIFEST.json`), which is a delete plus an add. Copying the changed files individually cannot express the delete, and the result is a tree with both manifests where the one git sees is never the one the generator updates. Apply as a patch (`git am`), and take `revendor_shared.sh --check` plus the full suite as the confirmation.

## Verification (Definition of Done)

* **Regression** — ✅ the three `*_to_md` test modules collect and pass; `requirements_docmd.txt` reflects where the code lives.
* **Drift** — ⚠️ `atrium_document.py` and `atrium_document.schema.json` byte-identical across the hub and all five tool repos, `v1` moved, para-drift green. *Not satisfied by editing the hub alone.* **This repo and the hub agree; the other four and the tag are outstanding.**
* **Ownership** — ✅ a digital-born document can originate `pages`/`content`/`lines`/`tables` under its own program identity, with truthful provenance; an origin/originator mismatch is refused on both `set_block()` and `merge_block()`; single-owner blocks behave exactly as before. Hardened in §0c: the refusal no longer depends on `set_source()` being called first, `own_fields` no longer confers writership, `merge_document_records()` no longer rewrites `source`, and an unrecognised origin spelling no longer disables the check in silence.
* **Data integrity** — ✅ output passes the canonical schema (`validate_document()`, pinned over a record carrying `pages`+`page_index`, `lines`+`bbox`+`group_id`+`style`, and `tables`+`cells`) **and** the round-trip assertion (`assert_fields_survived()`). One physical line is one row regardless of whether a writer passes `"1"` or `1`. PDF bounding boxes exact and in a **declared** coordinate space; DOCX bounding boxes explicitly absent and documented. Paragraph grouping carried by `lines[].group_id`; table cells joined by `cells[].group_id`, which is what makes the "lossless structured twin" claim implementable at all.
* **Fidelity actually delivered** — ✅ `group_id` is visible in the Markdown the model reads (blank-line paragraph boundary), and so are the page label, the canvas unit and the `needs_ocr` reason. A schema field no consumer reads does not count as done. **Remaining: `tables` → GFM.**
* **Licence** — ⚠️ the PDF geometry library is chosen on measured coordinate deltas (harness exists; the `sample.pdf` A/B is still to be run), and `provenance.license` for a digital-born document is what the project intends it to be — ✅ now `MIT` rather than `UNKNOWN`, because the components are declared in `para_config.txt`.
* **Reproducibility** — ✅ the golden fixtures are byte-identical across platforms and zlib versions, and the suite regenerates them and checks the pinned hashes rather than trusting a manifest nothing reads.
* **Pipeline** — CI proves the JSON is ingested by `atrium-llm-enrich` without failure. **CI does run pytest** (`docker-tool.reusable.yml`, `-m "not slow"` with a 42% coverage floor), so this is an enforced gate and not a local claim — #13 TODO 1's "no repo runs pytest in CI" is stale. 382 passed / 6 skipped once `requirements-test.txt` is actually installed in that job.
* **Migration** — the Issue #10 Markdown converters are regression-tested against the new JSON baseline and marked legacy.
