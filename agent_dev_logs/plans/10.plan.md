# Strategy — `ufal/atrium-llm-enrich` (Issue #10: PDF and DOCX inputs handling for DU)

> Issue [\#10](https://github.com/ufal/atrium-llm-enrich/issues/10) ("PDF and DOCX inputs handling
> for DU") · Labels: `development`, `integration` · Type: Task · Milestone: Q4 [WP3: Conclusion of Semantic Harmonization] ·
> Opened 2026-07-17 by K4TEL ·
> **Status: 🛠️ Implemented on `test`. First pass (§6, [`456ebb2`](https://github.com/ufal/atrium-llm-enrich/commit/456ebb2)):
> visually-rich Markdown converter for DOCX + digital-born PDF. Second pass (§7, 2026-07-22):
> unified the annotated-MD schema over TEITOK/ALTO, added the opt-in Tesseract OCR path, page-level
> citations, and pipeline auto-convert of `.pdf`/`.docx`. The §1–§5 research pass below is unchanged;
> OCR *tool selection* (PERO vs Tesseract) stays benchmark-gated under hub #22. Round 4 (2026-09-24, §8): TEITOK
> line-level input fixed (on `test`, `951db5e`). Route to Markdown (§9, 2026-09-24): stranak's 2026-09-23 question
> answered from a measured comparison — c) narrowed to #18's JSON route, b) flexiconv for the TEITOK plane, a) markitdown
> not adopted; reply text in §9, posted on the issue 2026-09-24 11:20 ([5813071038](https://github.com/ufal/atrium-llm-enrich/issues/10#issuecomment-5813071038)). The same measurement found the JSON route behind the
> older converters on eight points (G1–G8); §10 is the work plan that closes #10.**

## Context
Issue #10 asks how to turn **input PDFs of several kinds** — and **DOCX** as an alternative — into
**LLM-friendly inputs** (text + whatever layout signal we can keep). The issue names three PDF
classes plus two format asks:

1. **Curves instead of letters** — text drawn as vector outlines; no text layer, no fonts to map
   back to Unicode.
2. **Scanned images** — image-only pages, no text layer.
3. **Digital-born with text** — an extractable text layer already present.
4. **DOCX** as an alternative input format to PDF.
5. **DOCX / HTML as intermediate formats** on the PDF→LLM path (the issue's own suggestion).

**What "LLM-friendly" means in this repo.** Two existing entry points already define the target
shapes, so a converter should aim at one of them rather than invent a third:
- **Line-level** — `INPUT_DIR/*.csv` or `*.teitok.xml` (`file_id`/`page_num`/`line_num`/`categ`/
  `quality_score`/`text`), consumed by `llm_run.py` (local) and the `run_line_level()` path in
  `llm_client_shared.py`.
- **Document-level** — `.md`/`.txt`, consumed by `run_document_level()` in the remote/Ollama
  clients, today produced by [`api_util/xml_to_md.py`](../../api_util/xml_to_md.py) from TEITOK/ALTO.

So the pipeline already eats **TEITOK XML**, **CSV**, and **Markdown/plain text**. A PDF/DOCX
front-end's job is to land in one of those — most naturally **TEITOK** (line-level, keeps bboxes)
or **Markdown** (document-level).

**What already exists (and its limits).**
- [`api_util/flexiconv_convert.py`](../../api_util/flexiconv_convert.py) wraps UFAL's own
  [`flexiconv`](https://github.com/ufal/flexiconv) (pdf/docx/odt/rtf/html/md/txt/epub/… → TEITOK
  XML). It is a **standalone helper — nothing in the run pipeline imports it**, and
  [`para_config.txt:72-74`](../../para_config.txt) gates it: *"license not yet checked against
  project policy; flag before enabling txt/pdf/docx conversion."* (2026-07 finding: no licence in the
  README/repo metadata. **Update 2026-09-23:** `pyproject.toml` at the pinned `v0.3.10` declares
  `license = GPL-3.0-or-later`; there is still no LICENSE file — the gate is now a policy decision,
  not an unknown; since `f62921c` (on `test`) the row is a conditional `para_config.txt` component
  (`para_config.txt:37-42`). Also: flexiconv's TEITOK output has no `<s>`; the nlp-enrich reader fallback (rows
  from `<lb/>` lines or text blocks) is re-vendored in the same commit —
  `atrium-nlp-enrich/agent_dev_logs/plans/10.plan.md`. **Update 2026-09-24:** a `.teitok.xml` given as line-level
  input enriched zero lines — every TEITOK row was scored 0.0 and filtered as Trash; fixed the same day, see §8.)
- [`api_util/xml_to_md.py`](../../api_util/xml_to_md.py) is XML→text only (TEITOK/ALTO → page-sectioned
  Markdown). It is the house style for "→ LLM-friendly text" and the model a new converter should match.

**Relation to hub [#22](https://github.com/ufal/atrium-project/issues/22) (Document Understanding).**
#22 is the DU **evaluation** track (VLM-vs-legacy benchmark on in-domain Czech archaeological scans;
`bench_compare.py` + `sample_stratify.py` + `eval_metrics.py`). Its explicit guidance:
**do not build the ALTO→MD→LLM→TEITOK "Markdown-bridge" until the benchmark proves it is worth it**,
and it already noted **`opendatalab/MinerU`** as the parser candidate for that route. This issue (#10)
is the **input-ingestion** counterpart: the tools below are exactly the candidates #22 would
benchmark, so pick the routing here and let #22's metrics choose the winner per quality tier.

## Status snapshot
| Item                                            | State                  | Note                                                                                                                                                                                                     |
|-------------------------------------------------|------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Input taxonomy + detection heuristic            | ✅ **DECIDED**          | 3 PDF classes + DOCX; `pdffonts`/PyMuPDF font+text census routes digital-born vs. render+OCR (§1, smoke-tested)                                                                                          |
| Tool survey (parsers / OCR / VLM / DOCX)        | ✅ **DONE**             | §2 matrices, licenses verified                                                                                                                                                                           |
| Target format                                   | ✅ **RECOMMENDED**      | Reuse **TEITOK** (line-level) or **Markdown** (doc-level); HTML as a DOCX intermediate only (§3)                                                                                                         |
| Recommended routing for this repo               | ✅ **DRAFTED**          | §4 — router → per-class tool → existing dispatch seams; no premature Markdown-bridge (aligns hub #22)                                                                                                    |
| Smoke tests (CPU-light tools)                   | ✅ **RAN**              | §5 — 8 tools × fabricated fixtures for all 3 PDF classes + DOCX                                                                                                                                          |
| Visually-rich MD converter (DOCX + digital PDF) | ✅ **LANDED**           | §6 — `api_util/{layout_md,docx_to_md,pdf_to_md,doc_to_visual_md}.py` + 4 test modules, commit `456ebb2`                                                                                                  |
| Unified annotated-MD schema (TEITOK/ALTO)       | ✅ **LANDED**           | §7 — `xml_to_md.py --format layout` emits DOC_META/BBOX/PAGE_BREAK/figure cues from ALTO/TEITOK coords                                                                                                   |
| OCR path (scanned / curve-only PDF)             | ✅ **LANDED (opt-in)**  | §7 — `pdf_to_md.py --ocr`: pypdfium2 render + Tesseract `ces`; default still flags `NEEDS_OCR`. PERO next.                                                                                               |
| Pipeline auto-convert of `.pdf`/`.docx`         | ✅ **LANDED**           | §7 — remote/ollama clients pre-convert to cached `.md`; `llm_run.py` stays line-level                                                                                                                    |
| Page-level citation contract                    | ✅ **LANDED**           | §7 — doc schema returns `page`; prompt reads nearest PAGE_BREAK/## Page marker (issue #11 §3.3)                                                                                                          |
| README section for the converter                | ✅ **DONE**             | "Visually-Rich Document Input" section + install/OCR notes + input-mode table                                                                                                                            |
| flexiconv license                               | 🟡 **POLICY DECISION** | GPL-3.0-or-later declared in `pyproject.toml` (`v0.3.10`, checked 2026-09-23; no LICENSE file); `para_config.txt` component added (`f62921c`); services decided GPL-free 2026-09-24 — CLI policy remains |
| Live VLM/OCR on real AMCR pages                 | 🔄 **PENDING**         | Belongs to the hub #22 benchmark; no GPU/data in-session                                                                                                                                                 |
| TEITOK as line-level input                      | ✅ **FIXED**            | §8 — a missing quality score is "unknown"; GPU path passes it; re-vendored reader (2026-09-24, on `test` in `951db5e`)                                                                                   |
| Route to Markdown (stranak, 2026-09-23)         | ✅ **DECIDED**          | §9 — measured 2026-09-24: c) narrowed to #18's JSON route; b) flexiconv for the TEITOK plane; a) markitdown not adopted; reply ready                                                                     |
| JSON-route parity (G1–G8)                       | ✅ **DONE (files)**     | §11 (2026-09-25) — every §9 row closed on the JSON route except complex-page ordering (Phase 6); `tests/test_digital_parity.py`; delivered as files in chat, not pushed                                  |
| One entry point (#18 PR 7)                      | ✅ **ROUTE SWITCHED**   | §11 — auto-convert through the JSON route, cache version/engine/`--ocr`-stamped; legacy converters deprecated, NOT deleted (decision 2026-09-25); `--ocr` keeps `pdf_to_md` until Phase 3                |
| Shared page text-layer classifier               | ⏳ **PROPOSED**         | §10 Phase 4 — with alto-postprocess #31, via the hub shared template + para-drift                                                                                                                        |

---

## §1 — Input taxonomy & routing

The three PDF classes collapse to **two processing paths**, decided by a cheap, dependency-light
probe. A digital-born page has an extractable text layer and named fonts; curve-only and scanned
pages have neither, so both fall through to render-then-OCR.

**Detection heuristic (per page, then aggregate):**
- `pdffonts` (poppler) or `PyMuPDF page.get_fonts()` → **font count**.
- `PyMuPDF page.get_text("text")` → **extractable-char count**.
- `page.get_images()` → **image count** (a page that is one big image ⇒ scan).
- **Route:** `chars > threshold and fonts > 0` → **DIGITAL-BORN** (extract text layer);
  else → **RENDER + OCR** (covers curves *and* scans). A **per-page** decision matters: real reports
  mix a born-digital cover with scanned plates.

**Fourth, sneaky class — "extracts but garbled".** A digital-born PDF whose fonts are subsetted with
**no `/ToUnicode` CMap** will *extract* characters that are the wrong Unicode (diacritics especially).
This is not hypothetical — it showed up in our own smoke fixture (§5): text came out with `ě→I`,
`ř→I`, `ů→I`. Guard against it with a **decode-sanity check** (ratio of `U+FFFD`/control/out-of-script
codepoints, or a language-model/dictionary hit-rate on the extracted text); when it fails, treat the
page as OCR-path even though `chars > 0`. This is the single most important detail for Czech text.

Smoke-tested discrimination (fabricated fixtures, §5):

| fixture       | fonts | text chars | images | routed to              |
|---------------|------:|-----------:|-------:|------------------------|
| `digital.pdf` |     3 |        387 |      0 | DIGITAL-BORN (extract) |
| `curves.pdf`  |     0 |          0 |      0 | RENDER + OCR           |
| `scan.pdf`    |     0 |          0 |      1 | RENDER + OCR           |

## §2 — Tool survey

Columns: **Layout** = what structure survives (reading order / headings / tables / bboxes);
**OCR** = handles image/curve pages; **GPU** = needs a GPU for its default quality; **License** as of
2026-07. "Fit" is relative to this repo's TEITOK/CSV/Markdown targets.

### 2a. Integrated document parsers (PDF → Markdown/JSON, some also DOCX)
| Tool                               | Output                                                        | Layout                                   | OCR                                       | GPU                           | License                                                                                                              | Fit / notes                                                                                                                                                                                            |
|------------------------------------|---------------------------------------------------------------|------------------------------------------|-------------------------------------------|-------------------------------|----------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Docling** (IBM)                  | Markdown, HTML, **DoclingDocument JSON** (bboxes + hierarchy) | ★★★ headings/tables/reading-order/bboxes | yes (EasyOCR/Tesseract/RapidOCR backends) | optional (CPU ok; GPU faster) | **MIT** (models CDLA-Permissive/Apache)                                                                              | **Top pick for a general parser.** JSON carries bboxes → can be mapped to TEITOK. Also ingests DOCX/PPTX/HTML natively. Only snag in-session: pulls layout models from HuggingFace (blocked here, §5). |
| **MinerU** (OpenDataLab)           | Markdown, JSON (bboxes)                                       | ★★★ strong tables/formulas, CJK          | yes (built-in)                            | **yes** (VLM path)            | **MinerU Open Source License** (Apache-2.0-based; commercial OK below 100M MAU / $20M rev) — *relicensed off AGPLv3* | Already noted in hub #22 for the Markdown-bridge. Heaviest, best on hard layouts. Recheck the custom license against project policy.                                                                   |
| **Marker** (datalab)               | Markdown, JSON, HTML                                          | ★★★ (surya models)                       | yes (surya)                               | **yes**                       | **code GPL-3.0**; **models modified OpenRAIL-M** (free < $2M funding/rev)                                            | Fast + accurate, but **GPL + model revenue cap** are policy traps for a shared UFAL tool. Flag before adopting.                                                                                        |
| **markitdown** (Microsoft)         | Markdown                                                      | ★☆ text + basic tables; no bboxes        | no (delegates)                            | no                            | **MIT**                                                                                                              | Lightweight glue (uses `pdfminer`/`pdfplumber` for PDF, `mammoth` for DOCX). Great for **DOCX/HTML**; weak on scans and complex PDF tables (§5 flattened our table).                                   |
| **unstructured** (Unstructured.io) | element list (JSON) → text/MD                                 | ★★ element types, `hi_res` gives bboxes  | optional (Tesseract/PaddleOCR)            | optional                      | **Apache-2.0** (open core; some strategies/connectors gated)                                                         | Good partitioning + many formats; `hi_res` needs `detectron2`/OCR extras.                                                                                                                              |
| **pymupdf4llm** / PyMuPDF          | Markdown (+ word bboxes via PyMuPDF)                          | ★★ headings/tables/word-bboxes           | no (native text only)                     | no                            | **AGPL-3.0** (Artifex; commercial license sold)                                                                      | Fastest **digital-born** path, tiny deps; **AGPL** is the catch for a service. Good default for class 3.                                                                                               |
| **pdfplumber**                     | text, words+bboxes, tables                                    | ★★ word/char bboxes, table extraction    | no                                        | no                            | **MIT**                                                                                                              | Permissive, precise **word-level bboxes** → easiest bridge to TEITOK's line/token model. No OCR, no heading semantics.                                                                                 |
| **pypdf** / **pdfmin.six**         | text                                                          | ★ raw text                               | no                                        | no                            | BSD / MIT                                                                                                            | Lowest-level fallback; use only for quick text sniffing.                                                                                                                                               |

### 2b. OCR engines (the render+OCR path: curves + scans)
| Tool                                          | Output                                     | Layout                 | GPU         | License                  | Notes                                                                                                                                                                      |
|-----------------------------------------------|--------------------------------------------|------------------------|-------------|--------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **PERO-OCR** (BUT/DCGM)                       | text + **PAGE-XML** (regions/lines/bboxes) | ★★★ layout + baselines | GPU helpful | **BSD-3 / Apache-2.0**   | **Domain match**: built for Czech + historical print; permissive license; PAGE-XML maps cleanly to ALTO/TEITOK. Strong candidate for the OCR path here.                    |
| **Tesseract** (+ **OCRmyPDF**)                | text, hOCR, ALTO, searchable PDF           | ★★ hOCR/ALTO bboxes    | no          | Apache-2.0 / MPL-2.0     | Ubiquitous, **has a Czech pack (`ces`)**, emits **ALTO** directly (→ existing `xml_to_md.py`). Solid CPU baseline; OCRmyPDF wraps detect+skip+OCR. Verified working in §5. |
| **PaddleOCR** (PP-StructureV3 / PaddleOCR-VL) | text, structure, MD                        | ★★★ tables/layout      | yes for VL  | Apache-2.0               | Top OmniDocBench scores; heavier stack.                                                                                                                                    |
| **Surya** (datalab)                           | text + layout                              | ★★★                    | yes         | GPL + OpenRAIL-M (< $2M) | Marker's engine standalone; same license caveat.                                                                                                                           |
| **EasyOCR** / **kraken**                      | text (+ ALTO/PAGE for kraken)              | ★–★★                   | optional    | Apache-2.0               | EasyOCR = simple many-language; kraken = HTR/ALTO, the eScriptorium engine.                                                                                                |

### 2c. VLM transcribers (OOTB, for the hub #22 benchmark, not for blind production use)
Cross-referenced to hub #22 rather than re-decided here. Candidates: **olmOCR** (Apache-2.0),
**Qwen2.5-VL** (Apache-2.0 for ≤32B; 72B Qwen-license), **SmolDocling-256M** (Apache-2.0; emits
**DocTags**, Docling-native), **dots.ocr**, **DeepSeek-OCR**, and the domain-matched **CHURRO-3B**
already short-listed in `atrium-project/agent_dev_logs/plans/22.plan.md`. These produce Markdown/
structured text directly and are the "understanding" arm; **verify each weight license** before use
and let #22's CER/WER/entity-F1 per tier pick winners.

### 2d. DOCX-specific (issue asks #4 and #5)
| Tool                                   | DOCX →                      | Layout                                | License  | Notes                                                                                                               |
|----------------------------------------|-----------------------------|---------------------------------------|----------|---------------------------------------------------------------------------------------------------------------------|
| **mammoth**                            | **semantic HTML** (or MD)   | ★★★ headings/lists/tables from styles | BSD-2    | Style-aware, clean HTML — best "**HTML intermediate**" producer (§5 output was flawless).                           |
| **pandoc**                             | Markdown/HTML/…             | ★★★                                   | GPL-2.0+ | Universal, scriptable, exact GFM tables (§5). The terminal-friendly workhorse.                                      |
| **python-docx**                        | raw paragraphs/tables (API) | ★★ programmatic                       | MIT      | When you need field-level control, not a one-shot convert.                                                          |
| **LibreOffice** (`soffice --headless`) | PDF/HTML/DOCX (any↔any)     | ★★                                    | MPL-2.0  | Heavy but authoritative renderer; also the **PDF→DOCX/HTML intermediate** engine. (Finicky in this sandbox, §5.)    |
| **Docling / markitdown**               | MD (native DOCX readers)    | ★★–★★★                                | MIT      | Both eat DOCX directly — a dedicated DOCX branch may be a **one-liner** on top of whichever parser we pick for PDF. |

**Takeaway for #4/#5:** DOCX is the *easy* input — it has a real Unicode text layer, so mammoth/
pandoc/markitdown/docling all round-trip it near-perfectly (§5). "DOCX/HTML as **intermediate**"
(the issue's #5) is only worth it on the PDF side when a tool reconstructs structure better via DOCX
than via direct MD; in practice prefer **direct PDF→(MD or TEITOK)** and reserve DOCX/HTML
intermediates for genuinely DOCX-native inputs. Avoid `pdf2docx` (MIT) as a bridge — it is lossy and
adds a hop.

## §3 — Target format for the LLM

- **Markdown** — best token-efficiency/structure trade-off; what most parsers emit; lands directly on
  the **document-level** entry point. Use for whole-document enrichment.
- **TEITOK XML** — the repo's **line-level** native; keeps `page/line/bbox` so per-line `categ`/
  `quality_score` enrichment and paradata still work. Prefer when we need line granularity or bbox
  provenance (OCR path via PERO PAGE-XML / Tesseract ALTO → TEITOK).
- **HTML** — a faithful *intermediate* (mammoth/LibreOffice), not the LLM's final diet; convert on to
  MD/TEITOK.
- **DocTags** (SmolDocling/Docling) — richest structure, but couples us to Docling; fine internally,
  convert to MD/TEITOK at the boundary.

Recommendation: **do not add a new target format.** Route digital-born + OCR output into **ALTO/
PAGE-XML → TEITOK** (reusing `xml_to_md.py` and the TEITOK utilities) for line-level, or into
**Markdown** for document-level. This is exactly the seam `xml_to_md.py` already occupies.

## §4 — Recommended routing for `atrium-llm-enrich` (no code landed here)

A thin **router** in front of the existing dispatch, mirroring `flexiconv_convert.py`'s standalone
shape but producing the repo's native targets:

```
input file
  ├─ .csv / .teitok.xml / .md / .txt   → (unchanged) existing dispatch
  ├─ .docx / .html / .odt / .rtf       → pandoc|mammoth → Markdown (doc-level)   [+ Docling if adopted]
  └─ .pdf → probe (§1)
        ├─ DIGITAL-BORN  → pymupdf4llm/pdfplumber → Markdown/TEITOK
        │      └─ decode-sanity fails → treat as OCR
        └─ CURVES | SCAN → render @300dpi → OCR (PERO-OCR PAGE-XML | Tesseract `ces` ALTO)
                              → TEITOK (line-level) or Markdown (doc-level)
```

Integration seams (for the follow-up issue, not this one): the extension check in
[`llm_run.py:413`](../../llm_run.py) and the dispatch in
[`llm_client_shared.py`](../../llm_client_shared.py) (`read_input_rows` / `run_line_level` /
`run_document_level`). A converter should emit `.teitok.xml` or `.md` into `INPUT_DIR` so **no
dispatch change is needed** — pre-convert, then run, exactly as `flexiconv_convert.py` is meant to be
used.

**Tooling recommendation (permissive-first, matches UFAL policy pressure in `para_config.txt`):**
- **Digital-born PDFs:** `pdfplumber` (MIT, word-bboxes → TEITOK) or `pymupdf4llm` (faster MD, but
  **AGPL** — prefer pdfplumber for a shared service unless we license PyMuPDF).
- **Curves + scans:** **PERO-OCR** (BSD/Apache, Czech-tuned, PAGE-XML) as the primary; **Tesseract
  `ces`** (Apache) as the zero-GPU fallback that already emits ALTO for `xml_to_md.py`.
- **DOCX/HTML:** **pandoc** (GPL binary, used as an external tool — not linked) or **mammoth** (BSD)
  → Markdown.
- **General/complex parser (optional, evaluate in #22):** **Docling** (MIT) for the best
  structure-preserving MD/JSON when CPU-only permissive parsing is wanted; **MinerU** for the hardest
  layouts (recheck its custom license).
- **flexiconv:** keep as the one-shot convenience wrapper **once the copyleft policy is decided**
  (licence known: GPL-3.0-or-later); it already covers this whole matrix in one call, which is why that
  decision is the highest-leverage unblock. Its output is readable here since the 2026-09-23 re-vendor and
  enriched as line-level input since round 4 (§8). It writes no Markdown (§9).

Deliberately **not** doing now (per hub #22 risk note): building the full deterministic
ALTO→MD→LLM→TEITOK routing bridge. Land the router + one tool per class behind a benchmark first.

## §5 — Smoke-test results (reproducible)

Fabricated one Czech archaeological-style sample and derived all three PDF classes + a DOCX, then ran
the CPU-light tools. Fixtures: `digital.pdf` (reportlab, real text layer), `curves.pdf`
(`gs -dNoOutputFonts` → outlines, **reproduces issue bullet 1**), `scan.pdf` (rasterized image-only,
**bullet 2**), `sample.docx` (pandoc). Environment: `pdffonts`, `gs`, `tesseract 5.3.4` (+`ces`),
`pandoc 3.1.3`; venv with `pymupdf/pymupdf4llm 1.28`, `pdfplumber 0.11.10`, `markitdown 0.1.6`,
`mammoth 1.11`, `docling 2.113`.

| Tool                    | Input          | Text? | Layout kept                        | Verdict                                                                                                                                         |
|-------------------------|----------------|:-----:|------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------|
| PyMuPDF/pdffonts census | all 3 PDFs     |   —   | —                                  | **Routing works**: cleanly split digital vs curves/scan (§1 table)                                                                              |
| pymupdf4llm             | digital.pdf    |   ✅   | headings + **table** (MD)          | Best digital-born MD; fast                                                                                                                      |
| pdfplumber              | digital.pdf    |   ✅   | **word bboxes + table rows**       | Best for TEITOK bridge                                                                                                                          |
| markitdown              | digital.pdf    |   ✅   | headings lost, **table flattened** | Weak on PDF tables                                                                                                                              |
| tesseract `ces`         | **curves.pdf** |   ✅   | plain text                         | **Recovered full text where extraction got 0 chars** — the key result for class 1                                                               |
| tesseract `ces`         | scan.pdf       |   ✅   | plain text                         | Works; quality tracks render DPI                                                                                                                |
| mammoth                 | sample.docx    |   ✅   | **semantic HTML** (h1/h2/table)    | Flawless; best HTML-intermediate                                                                                                                |
| pandoc                  | sample.docx    |   ✅   | **GFM incl. table**                | Flawless                                                                                                                                        |
| markitdown              | sample.docx    |   ✅   | GFM incl. table                    | Flawless                                                                                                                                        |
| docling                 | sample.docx    |   ✅   | MD incl. table                     | Flawless (no model download for DOCX)                                                                                                           |
| docling                 | digital.pdf    |  ⚠️   | —                                  | **Blocked in-session**: pulls layout models from `huggingface.co`, denied by the environment network policy (403). Works where HF is reachable. |
| LibreOffice             | sample.docx    |  ⚠️   | —                                  | `soffice --headless` convert failed in this sandbox ("source file could not be loaded"); note as environment-specific, not a tool defect.       |

**Two findings worth carrying forward:**
1. **OCR is mandatory for class 1 (curves).** `curves.pdf` extracted **0** characters via every text
   parser; Tesseract `ces` recovered the whole paragraph. Curves and scans are the *same* path.
2. **Diacritic corruption on digital-born is real.** `digital.pdf` (non-embedded WinAnsi Helvetica,
   no `/ToUnicode`) extracted `sondě→sondI`, `hřeby→hIeby` across *all* text parsers — a systematic,
   not random, error. Hence the §1 **decode-sanity check**: born-digital ≠ trustworthy text for Czech.

## §6 — Implementation landed (2026-07-22, `456ebb2` on `test`)

Prompted by the issue's two 2026-07-21 comments (record page borders + "as many visual layout cues
as possible" as **HTML comments inside the Markdown**, with the exhaustive cue taxonomy in comment
#2 — cross-referenced from issue #11's format research). Scope, per the maintainer: **DOCX +
digital-born PDF now**, as a **standalone pre-convert CLI**, encoding a **pragmatic high-value
core** of the taxonomy. Landed in [`456ebb2`](https://github.com/ufal/atrium-llm-enrich/commit/456ebb2)
("add pdf/docx to md scripts"); the DEVLOG rebuild + refreshed issue exports followed in `9003639`.

New self-contained modules under `api_util/` (each a focused file, matching `xml_to_md.py` /
`flexiconv_convert.py`):
- **`layout_md.py`** — dependency-free cue vocabulary: pure emitters returning the *exact* strings
  from the taxonomy (`PAGE_BREAK`, `DOC_META`, `BBOX`, `FONT`, `STYLE`, `LAYOUT_MARGIN`,
  `HEADER_*`/`FOOTER_*`, `~~strike~~`, `<u>…</u>`, `<div align>`, footnotes, GFM `md_table`,
  `WATERMARK`, `LAYOUT_COLUMN`, `INDENT`) plus a `CUE_SCHEMA` catalogue — "the list of all possible
  visual layout pieces" made executable and single-sourced.
- **`docx_to_md.py`** — python-docx (MIT), lazy import: styles→headings, runs→emphasis + explicit-only
  FONT/STYLE cues, alignment, tables, section headers/footers + margins, hard page breaks →
  `## Page N` + `PAGE_BREAK`, image placeholders, best-effort footnotes.
- **`pdf_to_md.py`** — pdfplumber (MIT), lazy import: per-page `DOC_META`, block `BBOX`+`FONT`
  (blocks split on vertical gaps *and* font-size change so headings keep their size cue), tables,
  and the §1 **decode-sanity check** — text-less / garbled pages emit `NEEDS_OCR` instead of
  fabricated text (the §5 finding operationalised; the valid-but-wrong-diacritics case still needs a
  dictionary/LM hit-rate check, deferred with the OCR path).
- **`doc_to_visual_md.py`** — extension dispatcher + unified CLI (`input_file [--output out.md]`).

Deps: optional `requirements_docmd.txt` (python-docx + pdfplumber, both MIT — no `para_config.txt`
gate needed). Tests: `tests/test_layout_md.py` (pure, always runs), `tests/test_{docx,pdf}_to_md.py`
(hermetic — a tiny DOCX built in-process / a minimal hand-written PDF, `skipif` when the lib is
absent), `tests/test_doc_to_visual_md.py`. Full suite verified **255 passed, 3 skipped**, ruff clean,
on this exact code before it landed. **No dispatch change** — the CLI lands a `.md` in `INPUT_DIR`,
consumed unchanged by `run_document_level()`; HTML-comment cues pass through as inert text, and the
`## Page N` sectioning keeps the document-level locator prompt working.

**Still open after §6:** the render+OCR path for scanned / curve-only pages (flagged `NEEDS_OCR`,
benchmark-gated per §4 and hub #22); the flexiconv license blocker; a README section documenting the
converter (drafted, not yet committed); tool selection per quality tier via the #22 bake-off (see
issue #11's format decision in [`11.plan.md`](11.plan.md)).

## §7 — Second pass (2026-07-22): unify schema, OCR, citations, auto-convert

Four follow-on threads, all reusing `layout_md.py` (the single-sourced cue vocabulary). Full suite
**268 passed, 3 skipped**, ruff clean, on this code.

- **Unified annotated-MD schema.** `api_util/xml_to_md.py` gains a `--format layout` mode: new
  coordinate-aware readers (`_read_alto_layout` from ALTO HPOS/VPOS/WIDTH/HEIGHT; `_read_teitok_layout`
  aggregating `<tok bbox>` + `<surface lrx lry>` + `<figure>`) feed `rows_to_layout_markdown`, which
  emits the same `DOC_META`/`BBOX`/`PAGE_BREAK`/figure cues as the PDF/DOCX converter. The default
  `markdown`/`text` output is byte-identical (existing tests unchanged). This makes annotated MD the
  one LLM diet across PDF, DOCX, **and** TEITOK/ALTO (issue #11's decision, operationalised).
- **OCR path (opt-in).** `api_util/pdf_to_md.py --ocr` (and `doc_to_visual_md.py --ocr`): a
  `NEEDS_OCR` page is rendered with **pypdfium2** and transcribed with **Tesseract `ces`**
  (`pytesseract`), grouped into paragraph blocks with `BBOX` (rescaled to PDF points) and an
  `<!-- OCR: engine=tesseract, lang=ces -->` provenance cue (`layout_md.ocr_meta`). Lazy imports +
  graceful fallback: without the Tesseract binary the page just stays flagged. Default behaviour
  unchanged; the OCR *tool* stays benchmark-gated (PERO-OCR is a future backend at the same seam).
- **Page-level citation contract.** `build_document_schema` adds an optional `page` field;
  `_DOC_SYSTEM_HEADER` instructs the model to read it from the nearest `<!-- PAGE_BREAK: pg_N -->` /
  `## Page N` marker (and to treat layout cues as positional hints, never content);
  `run_document_level` surfaces `page` at the top of each record for `[Source: <doc_id>, Page N]`.
- **Pipeline auto-convert.** `llm_client_shared.prepare_document_input()` converts `.pdf`/`.docx` to a
  cached visually-rich `.md` (`_visual_md_cache/`, idempotent); wired into `openrouter_client.py` and
  `ollama_client.py` input enumeration (both gain `--ocr`). `llm_run.py` stays line-level only (no
  document-level path there by the #8 as-built design).

## Verification
- **§7 (this env):** `apt-get install tesseract-ocr tesseract-ocr-ces`; `pip install -r
  requirements_docmd.txt pytesseract`; `pytest -q` (268 passed, 3 skipped). Smoke: ALTO/TEITOK →
  `xml_to_md.py --format layout` shows cues while `--format markdown` is unchanged; a rasterised PDF
  → `doc_to_visual_md.py --ocr` recovers Czech text with an `<!-- OCR … -->` cue; a `.docx` dropped
  in a temp `INPUT_DIR` auto-converts to `_visual_md_cache/<stem>.md` and flows through
  `run_document_level` (record carries `page`).
- **§6 converter:** `pip install -r requirements_docmd.txt`; `pytest tests/test_layout_md.py
  tests/test_docx_to_md.py tests/test_pdf_to_md.py tests/test_doc_to_visual_md.py`. Smoke: build a
  DOCX / digital-born PDF → `python3 api_util/doc_to_visual_md.py <in> --output out.md` → confirm
  `## Page N` + `<!-- DOC_META/BBOX/FONT/PAGE_BREAK … -->` cues; a text-less PDF → `NEEDS_OCR`.
- **Reproduce §1/§5:** rebuild fixtures (reportlab + `gs -dNoOutputFonts` + PyMuPDF rasterize +
  `pandoc`), run the `pdffonts`/PyMuPDF census and the eight converters; the routing table and the
  "curves → 0 chars → OCR recovers" result reproduce deterministically.
- **When integrating (follow-up issue):** pre-convert a sample PDF/DOCX to `.teitok.xml`/`.md`, drop
  into `INPUT_DIR`, run `llm_run.py` (or a remote client) → confirm enrichment JSON + paradata, no
  dispatch change required.
- **Benchmark (hub #22):** feed the OCR-path and parser outputs through `bench_compare.py` against the
  gold set to choose tools per quality tier.
- **Policy decision (was: blocker):** flexiconv declares GPL-3.0-or-later (`pyproject.toml`, `v0.3.10`,
  checked 2026-09-23). ✅ The `para_config.txt` component exists (`:37-42`, `f62921c`). ✅ Services (2026-09-24,
  atrium-nlp-enrich #38): no image bundles flexiconv; they accept its `.teitok.xml` output. ⏳ Still to decide: CLI
  production runs. ✅ (`951db5e`) The `para_config.txt` row now states that no stage here converts with flexiconv, so it
  never enters a run's licence union in this repo (nlp-enrich's `api_flexiconv.sh` logs it where conversion happens).

## §8 — TEITOK as line-level input (round 4, 2026-09-24) — ✅ fixed the same day (on `test` in `951db5e`)
- ✅ **Zero lines enriched.** `read_input_rows()` scored every TEITOK row `0.0` (at `122915c`:
  `llm_client_shared.py:170-174`, `llm_utils.py:1846-1858`); the filter mapped < 0.40 to "Trash"; `stub_llm`
  patched the filter, so the suite stayed green; a CSV without `quality_score` was hit the same way. **Now:**
  `llm_client_shared.row_quality()` and `llm_utils._row_quality()` return None for a missing score, the filters
  skip the quality bands for it (only the length rules apply — the 06-15 design intent), the output record
  carries `"quality_score": null`, and `stub_llm` replaces only `make_chat_fn`.
- ✅ **GPU path:** `_should_process_line` was called with 6 of its 7 arguments, so every row raised and was
  swallowed. **Now** both call sites pass `_row_quality(row)`. The same two fixes in atrium-nlp-enrich's copy.
- ✅ **Re-vendored** `api_util/teitok_read.py` (per-page line numbers, page-split sentence rows),
  `api_util/flexiconv_convert.py` (exit 3 without flexiconv, 1 on a failed conversion) and its test; pins updated
  in `tests/test_vendored_teitok_parity.py`; nlp-enrich's released `CTX000000002` vendored as
  `tests/fixtures/teitok/writer/CTX000000002.teitok.xml`.
- ✅ `xml_to_md.py` writes `origin=printspace` in `DOC_META` when the writer's `appInfo` says the boxes are
  PrintSpace-relative, and renders a sentence crossing a `<pb/>` as one line per page.
- Evidence: `pytest -m "not slow"` 947 passed, 18 skipped. Record: [`13.plan.md`](13.plan.md) P5.

## §9 — Route to Markdown (stranak, 2026-09-23) — ✅ decided 2026-09-24 from measurements
The question: a) a broadly used tool (markitdown), b) UFAL's flexiconv, or c) the custom converters, "even if it
means maintaining them". The earlier draft of this section rested on §5 (markitdown 0.1.6, fabricated fixtures) and
code reading. It was re-checked against current releases and all routes were run on the same inputs.

**Setup.** Scratch venv; nothing in the repo changed. markitdown **0.1.8** (MIT, latest; `[pdf,docx]` → pdfplumber,
pdfminer.six, mammoth); flexiconv **0.3.10** (= `main` HEAD `a4f0fd9`, 2026-07-26; GPL-3.0-or-later; author Maarten Janssen);
this repo at `951db5e` through both of its routes — the **legacy** `doc_to_visual_md` → `pdf_to_md`/`docx_to_md` (the clients'
auto-convert) and the **JSON** `digital_to_json` → `json_to_md` (the hub e2e); plus alto-postprocess #31's
`text_formats.read_document()`. Inputs: `tests/fixtures/digital` (generated with `make_fixtures.py --outdir/--manifest` into a
scratch dir), alto-postprocess `data_samples/TEXT` `CTX000000010.docx`/`…11.pdf`/`…12.pdf`, `digital_born/sample.{pdf,docx}`
(two-column guide), and a DOCX with one tracked insertion and one deletion.

| Case                                                      | markitdown 0.1.8          | flexiconv 0.3.10                                        | legacy route             | JSON route                                           | alto-pp #31 reader  |
|-----------------------------------------------------------|---------------------------|---------------------------------------------------------|--------------------------|------------------------------------------------------|---------------------|
| Writes Markdown                                           | yes                       | **no** (`io/md.py` only reads)                          | yes                      | yes                                                  | no (text lines)     |
| Page boundaries                                           | none (stray `\f` at most) | `<div type="page">`                                     | ✅                        | ✅                                                    | ✅                   |
| Image-only page (`CTX000000011.pdf` p.3)                  | dropped silently          | empty div, no flag                                      | ✅ `NEEDS_OCR`            | ❌ scored Clear 1.0, vanishes                         | ✅ `text_layer=none` |
| `garbled.pdf` (`sondě→sondì`)                             | passed through            | passed through                                          | ❌ passed through         | ✅ page `NEEDS_OCR`; 2 of 3 bad lines still rendered  | ❌ `digital`         |
| `minimal.docx` heading / table / page break               | ✅ / ✅ / –                 | ✅ / ✅ / ❌                                               | ✅ / ✅ / ✅                | ❌ / ❌ / ❌ (stored, not rendered; break not captured) | – / cells / ✅       |
| `CTX000000010.docx` (no Override for `word/document.xml`) | ✅                         | ❌ "not a Word file"                                     | ❌ same                   | ❌ same                                               | ✅                   |
| Tracked insertion (`bronzová spona`)                      | ✅ kept                    | ❌ dropped                                               | ❌ dropped                | ❌ dropped                                            | ✅ kept              |
| Two-column `sample.pdf`                                   | prose → sparse tables     | partly tables, letter-spaced heads                      | table cells cut mid-word | words glued (`MALASTRANA’SMFF`), columns on one line | ✅ sensible order    |
| PDF geometry                                              | none                      | none with default `pdf=smart`; `pdf=bbox` needs Poppler | block bbox + font        | line bbox + page size                                | none                |
| OCR                                                       | Azure DI only (cloud)     | none                                                    | opt-in Tesseract `ces`   | none (flags `needs_ocr`)                             | none (classifies)   |

**Gap register — JSON route** (at `951db5e`):
- **G1** empty / image-only page scored Clear/1.0, no `needs_ocr`; `json_to_md` renders only pages with lines
  (`api_util/digital_to_json.py:377-403`, `assess_page`).
- **G2** glued words and merged columns — chars grouped by `round(top)` and joined with `""` (`:497-505`, `extract_pdf`).
- **G3** DOCX `lines[].style.heading_level` and `tables[]` stored but not rendered (#18 PR 5.5); explicit DOCX page breaks not
  captured by `extract_docx()` (`:569+`), which legacy `docx_to_md` and alto-pp #31 capture.
- **G4** python-docx refuses a package whose main part has no Override content type (`CTX000000010.docx`); flexiconv too.
- **G5** runs inside `<w:ins>` dropped by `Paragraph.text` — both our routes and flexiconv; mammoth and alto-pp keep them.
- **G6** on a condemned page, single-misread lines still render (`GARBAGE_MIN_HITS = 2`, `:161`).
- **G7** (code reading, no fixture) every PDF stamped `digital-born-pdf`; an OCR-layer scan belongs to `ocr:pdf-text-layer` /
  alto-postprocess, which #31 detects (`text_formats.py:448-460`).
- **G8** two entry points, two outputs: `prepare_document_input()` (`llm_client_shared.py:839`) → legacy route (misses the garbled
  check); hub `e2e-digital-smoke.yml` D1–D1c → JSON route; `_visual_md_cache` keyed on mtime only.

**Decision.**
1. **c), narrowed** — one route, PDF/DOCX → `atrium_document` JSON → Markdown. Maintained: the cue layer and renderer
   (`layout_md.py` + `xml_to_md.rows_to_layout_markdown` + `json_to_md.py`, ~1,100 lines, shared with the TEITOK path) and adapters
   over pdfplumber / python-docx. Not maintained: parsers. The legacy direct-to-MD pair (~800 lines) retires (§10 Phases 2–3).
2. **b) flexiconv for the TEITOK/NLP plane** and the long tail of formats, as a GPL CLI step; its TEITOK reaches the LLM through
   `xml_to_md --format layout`. G4/G5 reproducers can go upstream.
3. **a) markitdown not adopted** — no page markers, so no page citations (#11). Its lessons (lenient DOCX opening, tracked changes)
   are taken, not the dependency.
4. Complex layouts and the OCR engine are decided by the hub #22 harness, never by code here; anything adopted enters through the
   JSON IR.

## §10 — Work plan after the route decision (2026-09-24)
**Phase 1 — JSON-route parity** (#18 PR 5.5 + fixes; issue text drafted as "JSON route parity + retire legacy converters"):
- G1: a page with no text lines → `needs_ocr=True`, reason "no extractable text layer"; the renderer emits `## Page N` + the
  `NEEDS_OCR` cue for line-less pages (reuse `layout_md` emitters; keep the record schema-valid).
- G2: lines from pdfplumber `extract_words()` (spaces from x-gaps); split rows at column-sized gaps; order by column, then top.
  Anything beyond that waits for the Phase 6 bake-off.
- G3: `style.heading_level` → `#`×n; `tables[]` → GFM via `cells[].group_id` (grouped lines for merged cells, per Alfie's hub #22
  comment); explicit DOCX page breaks captured, reusing `docx_to_md`'s break handling.
- G4: on python-docx's content-type error, add the Override in memory and retry. G5: paragraph text from `.//w:t` without a `w:del`
  ancestor. G6: on a condemned page, ≥1 misread per line → Garbage.
- Fixtures in `tests/fixtures/digital/make_fixtures.py` (image-only page, two-column page, tracked-change DOCX, Override-less DOCX),
  regenerated `MANIFEST.json`, and a table-driven parity test.

**Phase 2 — one entry point** (#18 PR 7): `convert_to_visual_md()` for `.pdf`/`.docx` → `digital_to_json` (in memory) → the
`json_to_md` renderer; `iter_block_items` moves from `docx_to_md.py:55` into `digital_to_json.py`; `docx_to_md.py` and
`digital_born/` deleted; converter-version stamp in the `_visual_md_cache` name; `--ocr` stays on `pdf_to_md` until Phase 3;
README "Visually-Rich Document Input", `requirements_docmd.txt`, CONTRIBUTING.

**Phase 3 — OCR hand-off.** `needs_ocr` pages go to the OCR originator (alto-postprocess re-originates, per `ORIGIN_ORIGINATORS`);
engine from the hub #22 bake-off (Tesseract `ces`, PERO-OCR, #24 olmOCR, #25 archivatorium). `pdf_to_md.py` retires then.

**Phase 4 — shared page text-layer classifier** with alto-postprocess #31 (its empty / OCR-layer detection + pypdfium2 order; our
CP1250 mojibake table), shipped like `atrium_document.py` through the hub `docs/templates/shared/` + para-drift. Covers G7. Issue
text drafted as "shared page text-layer classifier".

**Phase 5 — flexiconv.** Upstream reproducers for G4/G5; CLI production-run policy stays with the team.

**Phase 6 — complex layouts.** Docling (MIT) / MinerU on the #22 harness with `sample.pdf` + AMCR pages
(`digital_born/sample.pdf_docling_ir.json` is a head start); adoption only as an adapter into the JSON IR.

**Verification.** `pytest -m "not slow"` (baseline 947 passed / 18 skipped), `ruff check .`, `make_fixtures.py --verify`; the §9
measurement re-run shows no ❌ in the JSON-route column except two-column ordering; hub `e2e-digital-smoke.yml` D1–D1c green; a PDF
and a DOCX in a temp `INPUT_DIR` give the same Markdown through `openrouter_client.py` auto-convert as through
`digital_to_json` → `json_to_md`.

**Closure of #10.** Reply posted and Phases 1–2 on `test`. The rest continues in #18, hub #22 / #24 / #25, #11, and the new
cross-repo classifier issue.

## §11 — Round 2026-09-25: Phases 1 and 2 done (with #18 §0e) — files in chat, not pushed
Every G-row of the §9 gap register, fixed in `digital_to_json` and its new modules (`digital_pdf.py`, `digital_docx.py`,
`digital_docling.py`, `digital_ir.py`), one fixture per gap in `make_fixtures.py`, and the §9 table as a test
(`tests/test_digital_parity.py`). Re-run of §9 on the JSON route, same inputs:

| Case                                        | JSON route, 2026-09-24                    | JSON route, 2026-09-25                                                                                                     |
|---------------------------------------------|-------------------------------------------|----------------------------------------------------------------------------------------------------------------------------|
| Page boundaries                             | ✅                                         | ✅ (+ PDF `/PageLabels`; DOCX pages from explicit, section and Word's rendered breaks)                                      |
| Image-only page (`CTX000000011.pdf` p.3)    | ❌ Clear 1.0, vanishes                     | ✅ `needs_ocr` "no extractable text layer: the page draws nothing a parser can see…", `NEEDS_OCR` cue rendered              |
| `garbled.pdf`                               | ✅ page flagged; 2 of 3 bad lines rendered | ✅ page flagged; 3 of 3 lines Garbage, none rendered                                                                        |
| `minimal.docx` heading / table / page break | ❌ / ❌ / ❌                                 | ✅ `###` heading / GFM table / `## Page 2`                                                                                  |
| `CTX000000010.docx` (no Override)           | ❌ "not a Word file"                       | ✅ read (content type repaired in memory; `.docm`/`.dotx` too)                                                              |
| Tracked insertion                           | ❌ dropped                                 | ✅ kept; deletion dropped                                                                                                   |
| Two-column pages                            | ❌ words glued, columns on one line        | ✅ words separated, simple two-column pages column-major (`two_column.pdf`); `sample.pdf`'s infographic pages still Phase 6 |
| PDF geometry                                | line bbox + page size                     | same, plus cell bboxes for ruled tables                                                                                    |
| OCR-layer PDF (`CTX000000012.pdf`)          | (not measured) stamped `digital-born-pdf` | ✅ refused, exit 3 — alto-postprocess's (`ocr:pdf-text-layer`)                                                              |

Also closed: G6 (condemned page), G7 (per-page OCR-layer census, pypdfium2), G8 (one route; cache stamped by converter
version, engine and `--ocr`). New: U+FFFD/control-character lines are Garbage (the legacy check the JSON route lacked);
French/Italian letters no longer condemn a line (alto-postprocess's `mojibake_line` guard). Opt-in heavy engine for
Phase 6 candidates: `--engine docling` (`requirements_digital_docling.txt`). Deleting `docx_to_md.py`, `pdf_to_md.py`
and `digital_born/` is a later round, by decision; Phase 3 (OCR hand-off) and Phase 4 (shared classifier — this round
ported alto-postprocess's rules so the two already agree) are unchanged.

---
_Research pass for issue #10 (2026-07-19); first implementation landed 2026-07-22 (§6, `456ebb2`); route decision,
measurements and work plan added 2026-09-24 against `test` `951db5e` (§9–§10).
Companion digest: [`../digests/10.digest.md`](../digests/10.digest.md) · format-decision sibling:
[`11.plan.md`](11.plan.md). Tool licenses stated as of 2026-07 and should be re-checked at adoption
time. §4's routing is the recommendation §6 began executing (DOCX + digital-born PDF); the OCR path
remains a benchmark-gated follow-up under hub #22._