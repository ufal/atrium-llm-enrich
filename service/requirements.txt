# service/requirements.txt — atrium-llm-enrich API service (strategy §4).
# The API reuses the torch-free enrichment engine (llm_client_shared + the
# openrouter/ollama clients), so it needs only the light remote stack plus the
# web server — never torch/transformers/vllm.
-r ../requirements_remote.txt
fastapi>=0.141.1
uvicorn[standard]>=0.52.0
python-multipart>=0.0.32
